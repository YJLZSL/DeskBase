# 贡献指南 · Contributing to DeskBase

感谢你愿意为 DeskBase（桌库）出力。无论是改一个错别字，还是重写同步引擎，都是这个项目需要的事。

本文档说明**如何参与**、**我们期待什么样的贡献**，以及**哪些红线不能碰**。第一次贡献前请完整读一遍，大约 5 分钟。

---

## 目录

- [一、承诺与行为规范](#一承诺与行为规范)
- [二、可以先做的几件事](#二可以先做的几件事)
- [三、环境准备](#三环境准备)
- [四、开发流程](#四开发流程)
- [五、编码规范](#五编码规范)
- [六、提交信息规范](#六提交信息规范)
- [七、测试要求](#七测试要求)
- [八、文档要求](#八文档要求)
- [九、Pull Request 流程](#九pull-request-流程)
- [十、项目红线](#十项目红线)
- [十一、设计提案（RFC）](#十一设计提案rfc)
- [十二、插件贡献](#十二插件贡献)
- [十三、翻译与本地化](#十三翻译与本地化)
- [十四、致谢](#十四致谢)

---

## 一、承诺与行为规范

参与本项目即表示你同意遵守 [CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md)。请对所有人保持尊重，技术分歧针对方案而非个人。

---

## 二、可以先做的几件事

刚接触项目、还不熟悉代码时，可以从这些开始：

| 类型 | 说明 | 标签 |
|------|------|------|
| 文档改进 | 错别字、表述不清、示例过时、缺少截图 | `docs` |
| 新手引导 | 复现别人的报错并补充到 FAQ | `good first issue` |
| 测试补充 | 给缺少覆盖的纯函数补单测 | `good first issue`, `test` |
| 翻译 | 界面文案、教程、文档本地化 | `i18n` |
| 无障碍 | 键盘可达性、屏幕阅读器、对比度 | `a11y` |
| 性能复现 | 按基准方法跑数据、贴出结果 | `performance` |

**不建议**新手直接动这些区域：崩溃恢复与 WAL 逻辑、加密与密钥管理、安装器注册表写入、同步协议。这些改动风险高，需要资深维护者评审。

---

## 三、环境准备

### 必需

| 工具 | 版本 | 说明 |
|------|------|------|
| Git | ≥ 2.40 | 版本控制 |
| Node.js | ≥ 20 LTS | 前端构建（若采用 JS 系技术栈） |
| 包管理器 | pnpm ≥ 9 / npm ≥ 10 | 统一用仓库声明的那个，不要混用 |
| Python | ≥ 3.11 | 仅当涉及工具脚本时 |

> 技术栈尚未最终冻结。仓库根目录会出现 `package.json` / `Cargo.toml` / `pyproject.toml` 等锁文件之一或若干；**以仓库实际存在的锁文件为准**，并严格使用其对应包管理器。混用包管理器会污染锁文件，是常见的 PR 被打回原因。

### 初始化

```bash
git clone <repo-url>
cd deskbase
<package-manager> install --frozen-lockfile   # 或 --ci / --locked
```

### 常用任务

仓库通过统一入口暴露任务，具体命令名以根目录脚本为准：

```bash
<pm> run dev        # 本地开发
<pm> run build      # 生产构建
<pm> run test       # 单元测试
<pm> run test:e2e   # 端到端测试
<pm> run lint       # 静态检查
<pm> run typecheck  # 类型检查
<pm> run format     # 格式化
```

---

## 四、开发流程

1. **先找 Issue**。要做的功能或修复，先确认已有 Issue；没有就开一个，说明场景与预期。
2. **领任务**。在 Issue 下评论「我要做」，维护者会分配给你，避免多人重复劳动。
3. **开分支**。从 `main` 切出，命名遵循：

   ```
   feat/<简短描述>      新功能
   fix/<简短描述>       缺陷修复
   docs/<简短描述>      文档
   refactor/<简短描述>  重构
   perf/<简短描述>      性能
   chore/<简短描述>     杂项
   ```

   示例：`fix/screenshot-ocr-memory-leak`

4. **小步提交**。一个 PR 只解决一件事。功能与重构不要混在同一个 PR 里。
5. **自测通过** 后再发起 PR。
6. **保持分支最新**。优先 `rebase` 到最新 `main`，保持线性历史；不要用 merge commit 同步上游。

---

## 五、编码规范

### 通用

- 遵循仓库根目录 `.editorconfig`：UTF-8、LF、空格缩进、文件末尾留空行、不留行尾空格。
- **禁止提交注释掉的死代码**，用版本控制来记住历史。
- **禁止提交调试残留**：`console.log`、`print`、`debugger`、断点、临时的 `TODO: 删掉这行`。
- 公共 API 必须有注释与示例，私有实现按需注释。
- **不允许**在代码中硬编码绝对路径、开发者机器名、内网地址、测试账号。
- **不允许**引入新的运行时依赖而不在 PR 中说明理由与体积影响。

### 命名

| 对象 | 约定 |
|------|------|
| 目录 / 文件（组件） | `kebab-case` |
| 变量 / 函数 | `camelCase` 或语言惯例 |
| 类型 / 类 | `PascalCase` |
| 常量 | `UPPER_SNAKE_CASE` |
| 数据库表 / 字段 | `snake_case`，表名用单数 |
| 布尔变量 | 以 `is` / `has` / `should` / `can` 开头 |

### 安全红线（写代码时就要守）

- **禁止 `innerHTML` / `dangerouslySetInnerHTML`** 直接拼接用户数据。渲染富文本必须走白名单净化。
- **禁止字符串拼接 SQL**。一律使用参数化查询或查询构造器。
- **禁止把密钥、令牌、密码写入日志**，日志中的敏感字段必须脱敏。
- **禁止引入会静默联网的依赖**（埋点、崩溃上报、CDN 字体、远程配置）。
- **禁止在渲染进程 / 前端页面直接持有文件系统或数据库句柄**，必须经主进程或本地服务的能力接口，并逐次鉴权。

### 性能红线

- 列表渲染必须使用虚拟滚动或分页，**不允许**一次性渲染上千行 DOM。
- 禁止在渲染主循环中做同步 I/O 或全表扫描。
- 定时任务必须有明确周期且可被合并/暂停，**不允许**秒级轮询。

### 提交前自查

```bash
<pm> run lint && <pm> run typecheck && <pm> run test
```

三项必须全绿。

---

## 六、提交信息规范

采用 [Conventional Commits](https://www.conventionalcommits.org/)，中文或英文均可，全仓库保持语言一致即可。

```
<type>(<scope>): <subject>

<body>

<footer>
```

| type | 含义 |
|------|------|
| `feat` | 新功能 |
| `fix` | 缺陷修复 |
| `docs` | 文档 |
| `style` | 格式（不影响逻辑） |
| `refactor` | 重构（不新增功能、不修缺陷） |
| `perf` | 性能优化 |
| `test` | 测试 |
| `build` | 构建 / 依赖 |
| `ci` | 持续集成 |
| `chore` | 杂项 |
| `revert` | 回滚 |

**示例**

```
fix(screenshot): 修复长截图在超高页面下拼接错位

滚动步长按视口高度计算时未扣除固定页头高度，
导致第二屏之后每屏重复 64px 内容。

Closes #412
```

**硬性要求**

- subject 不超过 72 字符，用祈使句，句末不加句号。
- 破坏性变更必须在 footer 写 `BREAKING CHANGE: <说明>`。
- 关联合并：`Closes #123` / `Fixes #123` / `Refs #123`。
- **禁止** `update`、`fix bug`、`修改`、`。` 这类无信息量的提交信息。

---

## 七、测试要求

### 覆盖面

| 改动类型 | 最低测试要求 |
|----------|-------------|
| 纯函数 / 工具方法 | 单元测试，覆盖正常、边界、异常三类输入 |
| 业务逻辑 | 单元测试 + 关键路径集成测试 |
| 数据库操作 | 集成测试（含事务回滚、并发写、迁移前向/回滚） |
| UI 组件 | 组件测试（渲染、交互、空态、错误态、loading 态） |
| 关键用户流程 | 端到端测试（如「新建表 → 导入 CSV → 查询 → 导出」） |
| 崩溃恢复 | **必须**包含断电模拟测试（进程强杀 + 重启后一致性校验） |

### 原则

- **先写复现用例**。修 bug 的 PR 必先有一个能失败的测试，再让 PR 把它变成绿的。
- **测试不得依赖网络**。所有外部依赖必须 mock 或使用本地 fixture。
- **测试不得依赖执行顺序**，每个用例独立可跑。
- **禁止**用 `sleep` 等待异步结果，用轮询断言或显式等待条件。
- 新增测试文件放在与被测代码同级的约定位置（如 `__tests__/` 或 `*.test.ts`）。

### 性能回归

涉及数据库、渲染、同步、启动路径的 PR，需在描述中附**改动前后**的基准数据（见 [docs/14-performance-budget.md](docs/14-performance-budget.md) 的测试方法）。性能回退超过 10% 的 PR 原则上不接受，除非有明确取舍说明。

---

## 八、文档要求

- 面向用户的功能变更，**必须**同步更新 `docs/` 下对应文档；没有对应文档则新建。
- 新增设置项，**必须**在设置文档中补充：名称、说明、默认值、影响范围、是否需重启。
- 架构级改动，**必须**新增一份 ADR（见 `docs/adr/`），模板如下：

  ```markdown
  # ADR-NNNN: <决策标题>

  - 状态：提议 / 已接受 / 已废弃 / 被 ADR-XXXX 取代
  - 日期：YYYY-MM-DD
  - 决策者：

  ## 背景
  ## 决策
  ## 备选方案
  ## 后果（正面 / 负面 / 中性）
  ## 参考
  ```

- 文档中的命令、路径、截图必须**真实可复现**，不允许「理论上」的示例。

---

## 九、Pull Request 流程

1. 按模板填写 PR 描述，包含：**做了什么 / 为什么 / 怎么验证 / 影响面 / 截图或录屏**。
2. 关联 Issue：`Closes #123`。
3. 确保 CI 全绿。CI 会跑 lint、typecheck、单元测试、构建、体积检查、依赖漏洞扫描。
4. 至少 **1 位维护者 approval**（涉及安全、加密、安装器、同步协议的改动需 **2 位**）。
5. 维护者可能请求修改。请勿在评审中用 force-push 覆盖已评审的提交，新增提交即可；合并前由维护者 squash。
6. 合并后你的名字会出现在 `CHANGELOG.md` 的 Contributors 中。

### 会被直接关闭的 PR

- 涉及仓库红线（见下节）。
- 一个 PR 混合多个不相关改动，且作者拒绝拆分。
- 无描述、无测试、无关联 Issue 的大型改动。
- 仅为「美化代码风格」的大范围重排，未先与维护者沟通。
- 引入体积显著膨胀的依赖而未说明理由。
- 包含许可证不兼容的代码或资源。

---

## 十、项目红线

这些是本项目的**不可协商**条款。违反者 PR 会被关闭，重复违反者会被限制参与。

1. **不得引入遥测 / 埋点 / 崩溃上报。** 任何形式的数据外发都必须由用户显式开启，且默认关闭。
2. **不得引入强制账号或强制登录。**
3. **不得在未告知用户的情况下联网。** 包括但不限于：更新检查、字体 CDN、图标接口、广告、统计分析。
4. **不得让用户数据离开本机**，除非用户对本次操作逐次显式授权。
5. **不得加入破坏「零污染」承诺的安装行为**：捆绑软件、静默安装第三方组件、强制重启、写入非必要注册表项、常驻后台服务。
6. **不得加入广告、推广位、付费墙。**
7. **不得破坏数据可导出性。** 任何新增的数据结构都必须提供开放格式的导出路径。
8. **不得使用与 Apache-2.0 不兼容的代码或资源**（如 GPL/AGPL 代码、非自由字体、无授权图片）。
9. **不得隐瞒数据行为。** 新增任何本地存储位置、权限申请、联网行为，必须在文档中明示。
10. **不得提交虚假的测试或性能数据。**

---

## 十一、设计提案（RFC）

涉及以下内容的改动，必须**先提 RFC 再写代码**，避免浪费：

- 新增或重命名一级模块
- 数据模型 / 表结构的破坏性变更
- 同步协议、加密方案、密钥管理
- 插件接口与权限模型
- 安装器行为与注册表写入策略
- 引入新的核心运行时依赖（如数据库引擎、本地推理引擎）

RFC 流程：

1. 在 Discussions 的 `RFC` 分类下发帖，标题 `RFC: <标题>`。
2. 使用 RFC 模板：背景 / 目标与非目标 / 方案 / 备选方案 / 迁移与兼容 / 安全与隐私影响 / 性能影响 / 开放问题。
3. 讨论期 **至少 7 天**。
4. 达成共识后由维护者转成 ADR 并合并入 `docs/adr/`。
5. 未获通过的 RFC 也应保留在 Discussions 中，作为决策记录。

---

## 十二、插件贡献

插件是 DeskBase 生态的重要组成部分。开发前请确认：

- 插件必须声明**权限清单**（文件系统范围、网络、剪贴板、数据库访问等），运行时按最小权限授予。
- 插件必须在**独立进程中运行**，崩溃不得影响主程序。
- 插件必须支持**一键关闭与完整卸载**，卸载后不残留文件、配置、后台进程。
- 插件必须提供**可审计的构建产物**（源码可获取、可复现构建、依赖清单）。
- 插件不得请求「全盘文件访问」「无限制网络」这类过宽权限；有正当理由须在描述中论证。

插件模板与接口定义见仓库 `plugins/` 与 `docs/15-open-source-governance.md`。

---

## 十三、翻译与本地化

- 界面文案统一放在语言资源文件中，**禁止**硬编码在代码里。
- 新增文案必须提供 `zh-CN` 与 `en-US` 两份；其余语言由社区维护。
- 翻译遵循「信达雅但优先清晰」：宁可朴素准确，不要华丽含糊。
- 涉及专有名词（SQL、WAL、索引、视图等）保持与 `docs/99-glossary.md` 一致。

---

## 十四、致谢

每一位贡献者都会被记录在 `CHANGELOG.md` 与仓库 Contributors 页面。

我们不要求你一次做出巨大改动。**一个准确的 bug 复现、一段更清楚的文档、一次性能数据的补充**，都是真实的价值。

欢迎来到 DeskBase。

---

<div align="center">

有问题？到 Discussions 里问。没人会因为你「问得太基础」而嘲笑你。

</div>
