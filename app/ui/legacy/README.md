# app/ui/legacy —— 已撤下但刻意保留的界面代码

**这些文件不再随应用分发**（不进 `assets.rs` 资源表、不被 `index.html` 引用），
留着是为了"可逆点"：恢复时把它们移回 `app/ui/` 并重新登记即可。

| 文件 | 撤下时间 | 依据 | 为什么保留 |
|------|----------|------|-----------|
| `sql.js` | 2026-09-20 | ADR-0020（表格优先形态，SQL 从产品移除） | 存储层未动，恢复 SQL 不需要动数据；将来若要"高级"入口，这里就是现成的 |
