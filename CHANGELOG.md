# 更新日志 · Changelog

本文件记录 DeskBase（桌库）的所有重要变更。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

变更分类：`新增` / `变更` / `废弃` / `移除` / `修复` / `安全` / `性能`。

---

## [未发布]

### 新增

- 初始化项目仓库与开源治理文件
  （`README.md`、`LICENSE`、`CONTRIBUTING.md`、`CODE_OF_CONDUCT.md`、`SECURITY.md`、`ROADMAP.md`、`GOVERNANCE.md`）
- 建立完整产品与工程方案文档体系（`docs/00` – `docs/17`）：
  - 总体方案摘要
  - 愿景、边界与核心原则
  - 目标用户与典型使用场景
  - 信息架构（一级模块 / 二级功能 / 三级设置）
  - 完整功能矩阵（MVP / 进阶 / 插件化）
  - 办公工具箱详细设计（含长截图、自定义快捷键、教程系统、设置界面）
  - SQL 数据库能力设计
  - 备份、恢复、断电保护与容灾设计
  - 隐私、安全、加密、权限与审计设计
  - 本地 AI 架构与隐私保护策略
  - 原生桌面端与手机远程端交互与同步架构
  - 安全与资源优化专项（安装、便携、卸载、注册表残留、后台进程、缓存与日志）
  - 安装界面、安装流程、卸载界面与卸载流程详细设计
  - 设置界面、教程系统、帮助系统详细设计
  - 性能优化清单与可量化指标
  - 开源治理、许可证、社区协作与插件生态建议
  - 分阶段路线图、风险、取舍与验收标准
  - 技术选型建议（候选方案 / 优缺点 / 适用条件，不绑定单一技术栈）
- 建立架构决策记录机制与初始 ADR
- 建立 Issue 模板、PR 模板与仓库协作约定
- 建立术语表，统一全项目名词

---


## [0.3.0-beta.1] - 2026-09-19

**主题：账本进得来，崩了不怕。**

v0.3.0 两大块交付：**Excel / CSV 导入建表**（发起人明确过"不是替代 Excel，
是要能导入 Excel"）与**崩溃恢复**（三条底线里"不丢数据"的最后一块）。
另外把程序内更新的前半段（检查 / 下载 / 校验 / 暂存）打通。

> ⚠️ **这是 beta：安装版、备份、默认模板库还没做。** 完整的 v0.3.0 交付清单
> 见 `local-docs/handoff/VERSION_PLAN.md` 第 3.2 节（本地文档，不随仓库分发）。

### 新增

**Excel / CSV 导入建表**
- 导入向导：铺出前 30 行让用户**点选表头行**（中文台账上面常压着标题行、导出日期行，
  程序猜错了他不一定看得出来）；每列给出**判断依据 + 置信度**（低置信度标红），
  可逐列改名 / 改类型 / 取消勾选
- 导入前先警告有坑的地方：合并单元格、15 位数字被 Excel 截断、1904 日期系统……
- **分批写入由前端驱动**：20 万行也不会让界面连同进度条一起冻住
  （那比没有进度条更糟）；失败或取消**不留半张表**
- 保真：前导零工号保住（001001）、金额按「分」存、11 位电话不变科学计数法
- 实测吞吐：1 万行 **74,627 行/秒** · 10 万行 **125,000 行/秒** ·
  20 万行 **109,230 行/秒**（14.9 MB CSV 写入 1.8 秒，分 40 批）

**崩溃恢复**
- 数据目录里的「未正常退出」标记（启动写、正常退出删）+ 启动完整性自检
  （`quick_check` + WAL checkpoint），自检**在打开数据库之前**跑
- 恢复向导：生成一致性快照（`VACUUM INTO`）/ 打开数据目录 / 知道了。
  **刻意没有「一键修复」** —— 向导的职责是让人看清现场，不是假装能把坏数据变好
- 强杀端到端测试（真实进程 · 三阶段）：写入 → 强杀 → 重启必进向导 + 数据一行不丢 →
  正常退出后**不再误报**。实测已提交 150 行，重启后 150 行

**程序内更新（前半段，ADR-0018）**
- 版本检查：GitHub Releases API，稳定 / 预发布双通道，默认稳定通道
- HTTPS 下载走系统 WinHTTP（不引入 TLS 依赖，体积优先）
- 下载后**五步校验**与暂存；替换动作由新版本 exe 执行（无签名前提下的设计）

**界面自动化测试体系（不替代人，但把能机器化的都机器化了）**
- 界面烟测：真实 WebView 里真实点击 **31 步**（导航 / 更新卡 / 数据库建表录行读回 /
  导入向导 / 进度条三态）
- 端到端验收：**28 步**真实数据走完整导入链路（选文件 → 认表头 → 改列 → 分批落库 →
  左栏出现 → 读回 → 删表）
- 压力测试：**22 项 × 三档**（1 万 / 10 万 / 20 万），含分页不重不漏、排序、筛选、聚合
- 自动点击走查：外部 DevTools 协议驱动 —— **18 张截图** + 逐页文案导出 +
  4 档窄屏（1000/800/640/520）布局体检，产出可审报告

### 变更

- 版本线重排：`v0.3.0` 拆出「Excel 导入建表 / 默认模板库 / 简洁模式 / 教程扩写」
  四项为第一优先级（发起人 2026-09-18 明确要求），表结构编辑与可靠性套件顺延
- **导入的空值语义拍板（Q-047）**：导入时空单元格统一存 `NULL`（**文本列也不例外**）；
  手动编辑保留「文本格清空 = 空串」的既有语义。两条路径的差异是**刻意**的 ——
  源文件的空格子只有一个含义（"没填"），而"空串"是手动编辑时用户能主动表达的意思

### 修复

- 「日期时间」字段类型**选了就报错**（serde 序列化成 `date_time`、界面传 `datetime`）
  —— 存在已久，此前没有任何测试用这个类型建过表
- 20 万行导入上限的**边界判错**：闸门按整表行数卡，导致「2 行标题 + 1 行表头 +
  恰好 20 万行数据」被拒 —— 少一行能过、恰好到上限反而不过
- **构建时弹控制台窗口刷乱码**（`spawnSync` 缺 `windowsHide`）—— 用户反馈的现象
- **更新检查在系统代理不可用时整体失败**：系统代理（代理软件）挂着时，
  `AUTOMATIC_PROXY` 把每次请求都交给那个死代理 —— 实测检查更新 5/5 全失败
  （0x80072EFD），而同一时刻直连 api.github.com 完全正常。现在改为**双路兜底**
  （系统代理 → 直连，本次运行内记住哪条通），并且 403/429 也换路重试
  （GitHub 匿名限额按 IP 算，代理出口是共享 IP）。附带修正了**三条错误码翻译**
  —— 12029 被错标成"TLS 握手失败"，把"代理挂了"显示得像"证书出问题"
- 发布工具的「待推送数量」不再依赖 `origin/main` 这个可能不存在的本地引用
- 联网测试不再"随机红"：两条路都是传输层失败时如实打印原因并降级（环境问题），
  HTTP 应答错误与校验失败仍然必须红

### 数字

`cargo test` **267 / 267**（上一版 172）· 编译零警告 · release **6.164 MB** ·
界面烟测 31/31 · 端到端 28/28 · 崩溃恢复 16/16 · 压力 22/22 × 3 档 ·
联网检查（走兜底）实测通过 · 走查 7/7（布局疑似 0 处）· 下载 e2e 在 CDN 不可达时优雅降级 ·
五个门禁全过（接线 / 动效 / 对比度 / 体积 / 字段名）

---

⚠️ **仍然没有代码签名证书** —— 运行时 SmartScreen 会拦（点「更多信息 → 仍要运行」）。
这是已知且不会自动消失的障碍，不假装没有。

⚠️ **界面层的人工走查仍未做**：自动走查把"值得看的东西"整理好了
（截图 / 文案 / 布局疑似清单），但"上手用一遍"替代不了。


## [0.2.1] - 2026-09-19

**主题：修与验。** 把 beta 发出去却没人点过的东西修稳、验实。
`cargo test` **172 全过**（基线 161 → +11）、编译零警告、三个门禁全过、
实机自检 0 资源未命中、release **5.952 MB**。

### 修复

- **SQL 耗时完全不可见（P1）**：`app/ui/sql.js` 读的是 `res.elapsedMs`，
  而 Rust 侧 `QueryResult` 序列化出来的是 `elapsed_ms` —— 与 `has_more` / `hasMore`
  是同一类事故：**值恒为 `undefined`，界面毫无异常**。
  后果是每条语句的耗时徽章、结果块的总耗时、以及「超过 1 秒请当心结果不完整」的提醒全部不显示
- **接线门禁的「假绿」**：`check-wiring.cjs` 第 4 项（前端调用的 IPC 命令是否都有分支）
  原先直接扫 `main.rs` 原文，**一句注释里提到 `"schema.runQuery" =>` 就能让它变绿**。
  现在先剥离 Rust 注释（字符串内容保留），并额外认 `cmd == "x.y"` 形式 ——
  门禁该适配代码，而不是逼着代码去迎合门禁

### 新增

- **SQL 执行超时与中断（Q-042）**：`schema.runQuery` 改为**异步**。
  主线程解析参数 → 过危险语句闸门 → 登记 `Connection::interrupt_handle()` → 起工作线程后立刻返回，
  **界面不再被慢查询冻住**。执行中再点一次「执行」= 中断；超过 `timeoutMs`（默认 30 秒）
  由超时哨兵自动按下中断。报错不是「查询失败」四个字，而是把原因（条件列没索引 / 全表扫描）
  和下一步讲清楚。**忙时明确拒绝第二条查询而不是排队** —— 排队会让「哪条结果对应界面上哪个结果块」
  变得不可预测。拿不到事件循环时（如单元测试）明确报错，不静默挂起
- **建表向导的默认值输入框**：`DEFAULT` 白名单早已就绪，这次把界面补上。
  关键词（`CURRENT_TIMESTAMP` / `NULL` / 数字）原样落库，普通文本自动加引号；
  **金额列的默认值按「元」输入、按整数「分」落库**，与金额列本身的存储口径一致
- **数据库页验收自动化（11 个集成测试）**：把 `ENV_SETUP.md` 第 4 节那 16 步验收里
  能用 IPC 表达的部分变成**可重复运行**的测试 —— 建表含金额列 → 金额往返（含三位小数被拒）→
  250 行 keyset 翻页不重不漏 → 三态排序 → 列筛选 → 改单元格与 NULL → 批量删行 →
  危险语句双重闸门（含 `WHERE 1=1` 恒真谓词）→ 忙时拒绝与中断可达 →
  工作区状态「重开」后恢复 → 建表默认值落库。
  走的**是真正的分发入口**（`dispatch`），不是绕过 IPC 直接调内部函数
- **跨语言字段名通用门禁**：原先只盯 `Page` 的 `has_more` 一条，现在对所有 serde `snake_case`
  字段禁止在 `db.js` / `sql.js` 里出现驼峰写法（检查前先剥离注释，免得门禁被自己的说明文字弄红）
- **数据库默认模板**：建表对话框顶部可套用「客户台账 / 进出货 / 费用报销 / 库存盘点」，
  字段与类型已配好（金额按「分」、日期用日期型），表名与字段都能改。不用再从空白表开始发呆
- **`node scripts/check-wiring.cjs` 接线门禁**：检查四项 ——
  ① `index.html` 引用的站内资源是否都在 `assets.rs` 登记
  ② `app/ui/*.js` 是否都挂进了 `index.html`（写了没挂 = 死文件）
  ③ `app/ui/*.css` 是否都进了 `check-motion` 的 TARGETS
  ④ 前端调用的 IPC 命令是否都在 `main.rs` 的 `dispatch()` 里有分支。
  这四项都不是编译错误，只能靠门禁拦（已做负向验证：故意移出一项确实变红）

### 文档

- ADR-0017（AI 协助的隐私边界）：默认本地、不许静默降级到云端、外部调用逐次授权、
  默认只发结构信息不发数据行、AI 生成的内容不自动执行、没做到之前界面不放 AI 入口
- 新增 `local-docs/handoff/ACCEPTANCE-2026-09-19.md`：数据库页 16 步验收记录，
  **逐项标注「已自动化 / 仅 IPC 层 / 未覆盖」**，不把「测过后端」说成「点过界面」

### 已知未完成

- **界面层（真实的鼠标点击路径）仍未经人工走查**：上面的自动化覆盖的是 IPC 与数据库这一层；
  「双击单元格」「勾选两行」「表头点第三次回到无排序」这些**前端状态机**没有被测到
- 16 步验收里的第 16 步（一键创建示例台账）没有测试覆盖
- 14 / 15 步用「换一个 `AppState` 打开同一份库」模拟重启，**真实进程重启未做**
- SQL 执行那条异步链路在单元测试里拿不到事件循环，测到的是闸门与拒绝逻辑，
  **执行与回传需要实机验证**（已实机启动自检通过）
- 其余同 beta.3：无安装版、无签名、无自动更新

---

## [0.2.0-beta.3] - 2026-09-18

围绕「更新与升级时不丢用户数据」的一批，`cargo test` 161 全过、编译零警告、release 5.906 MB。

### 新增

- **数据自动升级**：数据库结构逐级迁移，每级一个事务；**每升一级前用 `VACUUM INTO` 把整份库备份到
  `<数据目录>/backups/before-v<N>-<时间戳>.db`**。备份失败直接中止，迁移失败回滚且 `user_version` 不变，
  报错会指向备份文件位置
- **工作区状态持久化**：当前视图 / 打开的表 / 侧栏状态 / 窗口尺寸存进库，下次启动自动恢复。
  **覆盖 exe 或解压新便携包，这些都不丢**
- **退出前保存**：关闭窗口时先通知前端把没写完的内容落库（400ms 宽限，不会因此关不掉），
  再把工作区状态落盘后退出
- **设置页「数据库使用教程」**：8 节 —— 数据库是什么（白话）/ 三步建一张台账 / 9 种字段类型怎么选 /
  查找与筛选 / 改与删 / 三个能直接抄的 SQL 例子 / 常见错误对照 / 术语表
- **数据库页首次使用引导**：一张表都没有时给出三步引导，并可一键「创建示例台账」立刻看到效果

### 变更

- ADR-0016（程序内更新：默认关闭、显式开启、强制签名校验、更新不碰数据）已提交，**状态：提议，待确认**

### 已知未完成

- **自动更新只有设计没有实现**（ADR-0016 待确认 + 依赖代码签名证书）
- 数据库页的交互验收**仍未人工执行**
- 其余同 beta.2

---

## [0.2.0-beta.2] - 2026-09-18

修掉 beta.1 里两个只有真用起来才会暴露的问题，`cargo test` 153 全过、编译零警告。
**如果已经下载了 beta.1，请换成这一版。**

### 修复

- **超过 200 行的表翻不了页**（P1）：`db.js` 把分页应答的 `has_more` 写成了 `hasMore`，
  该值恒为 `undefined`，于是「加载更多」永远不触发。测试、界面自检、日志**全都显示正常** ——
  只有真的打开一张超过一页的表往下滚才会发现
- **危险 SQL 闸门可被 `WHERE 1=1` 绕过**（P2）：`DELETE FROM t WHERE 1=1` 有顶层 `WHERE`，
  此前会被当成"有条件"而不弹确认，直接全表删。`has_where` 现在会评估谓词：
  恒真写法（`1` / `true` / `1=1` / `(1=1)` 及其纯恒真的 AND 组合）一律视为没有过滤条件；
  判断不了时按"需要确认"处理（宁多问一次）
- `refreshTables` 加并发守卫：快速切换视图不再重复拉表列表

### 安全

- 新增回归门禁测试「分页应答的字段名与界面读取的名字一致」：把 Rust 侧序列化出的键
  与 `db.js` 读取的名字两头钉住，**已做负向验证**（改回 `hasMore` 时测试确实变红）。
  跨语言边界传字段这类静默失效，以后不许再靠眼熟

### 已知未完成

同 beta.1；另新增一条：数据库页的交互验收**仍未人工执行**

---

## [0.2.0-beta.1] - 2026-09-18

**数据库模块正式上线 —— 本项目的核心能力（内置 SQL 数据库）从这一版起可用。**
`cargo test` 151 个用例全过、编译零警告、门禁全绿。

### 新增

- **「数据库」页上线**（此前为占位卡片）：
  - 可视化**建表向导**：表名 + 字段列表，9 种字段类型（文本 / 整数 / 小数 / 金额 / 是否 / 日期 / 日期时间 / JSON / 二进制），支持主键与必填
  - **数据网格**（`grid.js`，2,239 行）：虚拟滚动（复用 DOM 行池）、表头排序三态、列筛选（包含匹配，与 keyset 游标分页可叠加）、单元格就地编辑、NULL 与空串分家显示、批量删行、表尾新增行
  - **SQL 编辑器**（`sql.js`，1,947 行）：自实现语法高亮、表名字段名补全侧栏、执行历史、多语句逐条执行
  - **删表**需逐字输入表名确认
- **策略层危险操作闸门**（IPC 边界）：`DROP` / `TRUNCATE` / 无顶层 `WHERE` 的 `DELETE` / `UPDATE` 必须带着显式确认才执行；前端确认框由 `needsConfirm` 应答协议驱动
- `has_where` 升级为**括号深度感知**：`UPDATE t SET a=(SELECT 1 WHERE 1=1)` 这类 WHERE 藏在子查询里的全表改写，现在会被正确拦下确认（第十二轮交接点名的确认漏网）
- 新增 IPC 命令 10 条：`schema.listTables` / `getTable` / `columnMeta` / `createTable` / `dropTable` / `pageRows` / `updateCell` / `deleteRows` / `insertRows` / `runQuery`
- 界面自检覆盖扩展：数据网格 / SQL 编辑器 / 数据库页三个模块纳入 `app.diag`

### 修复

- 筛选列不存在时明确报错，不再静默当作"无此条件"

### 性能

- 大表浏览走 **keyset 分页**（上限 10,000 行/页），不用 `OFFSET`；排序键含并列值与 NULL 时不重不漏
- 单次 SQL 结果集上限 5,000 行（可在 1–100,000 之间调），防止 `SELECT *` 卡死界面

### 安全

- 渲染层仍然零数据库直访：所有库表操作经 IPC 白名单命令；表名/字段名过标识符校验后由 Rust 侧转义，值一律参数绑定
- SQL 内容不写日志（隐私承诺不变）；"执行过写操作"这一事实记入审计日志

### 已知未完成

- **表结构编辑**（改表名 / 加列 / 改注释）：后端 API 与测试已就绪并显式标注，界面未做（建表向导二期）
- SQL 执行**没有超时机制**（`get_interrupt_handle` 未接线）—— 慢查询只能等它跑完
- 数据库页的交互（建表 → 翻页 → 编辑 → 筛选 → 删行）**尚未人工过一遍**，自检只证明模块加载成功
- 安装版 / 代码签名 / 自动更新 / 长截图 / 业务表 / AI 功能：见 [0.1.0-alpha.1] 的清单，状态不变

---

## [0.1.0-alpha.1] - 2026-09-17

首个可运行的 alpha。**桌面端垂直切片打通：外壳 / 存储 / 界面 / Excel 双向 /
格式转换 / 截屏 / 命令面板 / 主题与动效 / 导入管线。**

> ⚠️ **这是 alpha，请勿用于唯一的生产数据。** 已知未完成的部分列在本节末尾。

### 新增

**外壳与界面**
- Rust + 系统 WebView2 的桌面外壳（Windows 优先），单文件 exe，无运行时依赖安装
- 界面资源走自定义 `deskbase://` 协议：**渲染层无文件系统直访**，只服务编译期固定的资源表
- **11 个内置主题**，按四个流派家族组织（宣纸 / 青瓷 / 竹青 / 朱墨 / 夜墨 / 松烟 /
  玄铁 / 高对比亮 / 高对比暗 / 等宽绿 / 粗野），主题改的是**结构**而不只是色相
- 四档动效强度（关 / 精简 / 标准 / 丰富），跟随系统的「减少动效」自动封顶
- 标题槽位内嵌**得意黑 Smiley Sans**（SIL OFL 1.1，随程序分发、不联网下载）
- **命令面板**：`Ctrl+K` 唤起，支持**拼音首字母**搜索（`xjbj` → 新建笔记）
- 基础组件库：Toast 栈（可带撤销）、模态框（含焦点陷阱）、骨架屏、开关、工具提示、滚动边缘阴影
- 三条从物理参数采样出的 **CSS `linear()` 真弹簧**曲线（临界阻尼 / ζ=0.85 / ζ=0.70）

**数据**
- SQLite（bundled，WAL + `synchronous=FULL`）单文件存储，软删除
- **Excel 导出**：长编号（订单号 / 身份证 / 银行卡）按文本写入，不会被 Excel 变成科学计数法
- **Excel / CSV 导入前的静默毁数据检测**，逐个点名这七类坑：
  Excel 的 15 位有效数字截断、1904 日期系统（差 1462 天）、1900 假闰日、
  `<dimension>` 炸弹、合并单元格、公式缓存值过期、全角数字与 GBK 编码
- **CSV 编码探测**：BOM → 严格 UTF-8 校验 → GB18030 回退。
  专门为「中文 Excel 的另存为 CSV 输出的是 GBK 不是 UTF-8」这一条设计
- 导入落库管线：分批提交、进度与数据同事务、**可撤销**、跨进程强杀后可报告进度

**办公工具**
- **格式转换**（纯离线）：xlsx ↔ csv/tsv、png ↔ jpg/bmp/webp、GBK ↔ UTF-8 ↔ UTF-16。
  **两步式**：先给出「会丢掉什么」的清单（具体到数量与名字），确认后再执行
- **截屏**：按物理像素抓取，DPI 感知（125% / 150% 下不模糊）
- **批量导入**：从表格批量建笔记

### 安全

- **零出网、零遥测**。唯一的联网能力是「用系统浏览器打开项目仓库 / 发布页」，
  地址由 Rust 侧**硬编码白名单**限定，越界的请求会被拒绝并写进审计日志
- 文件路径只在 Rust 侧流转：原生对话框在 Rust 里打开，渲染层既给不了路径也拿不到路径，
  因此**渲染层没有「读任意文件」的能力**
- 所有转换与导出**只写新文件**，已有文件一律不覆盖，绝不原地修改用户的原件
- 截图内容不上传、不记录，日志里只有尺寸

### 已知未完成（这一版明确没有的东西）

- **没有安装包**。首发只出便携版。自建安装器涉及注册表写入、卸载残留、
  WebView2 bootstrapper 探测，都有明确验收标准，没达标就不发
- **没有代码签名证书**，所以 Windows SmartScreen 会拦安装包
  （便携版受影响较小）。这也是将来更新器会被 Defender 误报的唯一真正解法
- **没有自动更新**。更新是「打开系统浏览器 → 到发布页手动下载」
- **长截图的滚动拼接还没接线**。抓屏与拼接算法（含相位相关的位移估计）
  已经写好并有测试，缺的是「界面滚一屏 → Rust 抓一帧」的往返协议
- **数据库模块只落地了笔记**。物流 / 财务 / 工厂收发那些场景的表
  （单据明细、库存流水、月结快照）设计已完成但尚未建表
- **AI 功能一个都没接**。本地语义搜索、OCR、分类打标签的选型与隐私
  方案已经定稿，实现排在后续版本
- 字体自定义导入（.ttf / .otf / .woff2）界面还没做，目前只有内置预设

### 工程

- `cargo test` **103 个用例**，覆盖 Excel 往返、CSV 编码、格式转换、
  截屏拼接、崩溃恢复
- 四道 CI 门禁：测试、动效合规（只动 transform/opacity）、
  主题对比度（11 主题 × 15 组 WCAG）、界面脚本语法
- 编译零警告，且 CI 用 `-D warnings` 把它变成硬失败
- 发布脚本产出便携 zip + SHA-256 + **CycloneDX SBOM**（149 个组件，
  按 Windows 目标平台过滤，不是全平台并集）

## 版本发布规则说明

- 项目已发布首个 alpha（`0.1.0-alpha.1`，2026-09-17）。**alpha 不算「正式版本」**：
  `0.x` 期间不承诺向后兼容，数据结构与界面都可能变。
- 正式版本发布后，本文件将按下述结构记录：

  ```markdown
  ## [1.2.0] - 2026-06-01

  ### 新增
  - ...

  ### 修复
  - ...

  ### 安全
  - ...

  ### 性能
  - 万行表格滚动帧率由 42 FPS 提升至 58 FPS（#789）

  ### 破坏性变更
  - 插件 API `host.db.exec` 已重命名为 `host.db.query`，旧名将在 2.0 移除（#790）
  ```

### 破坏性变更的声明要求

任何 `MAJOR` 版本或含破坏性变更的发布，必须：

1. 在 `CHANGELOG.md` 中以 **破坏性变更** 小节明确列出；
2. 在对应文档中给出**迁移指南**（旧格式 → 新格式、旧 API → 新 API）；
3. 至少提前 **一个次版本** 公告，并在运行时给出可操作的迁移提示；
4. 数据格式变更必须提供**自动迁移 + 迁移前自动备份 + 迁移失败回滚**。

---

## 链接

- [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)
- [语义化版本](https://semver.org/lang/zh-CN/)
- [项目路线图](ROADMAP.md)
- [贡献指南](CONTRIBUTING.md)
