# 更新日志 · Changelog

本文件记录 DeskBase（桌库）的所有重要变更。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

变更分类：`新增` / `变更` / `废弃` / `移除` / `修复` / `安全` / `性能`。

---

## [0.4.0] - 2026-09-21 · 去掉 SQL，换成「多表 + 关联 + 协同更新」

> 这是一次方向性的重构，不是一个功能补丁。DeskBase **不再内置 SQL 引擎**，
> 详见 [ADR-0021](docs/adr/0021-remove-sql-own-storage-engine.md) 与
> [ADR-0022](docs/adr/0022-linked-tables-and-sync-rules.md)。

### 移除

- **SQL 与 SQLite 全部移除**：`rusqlite` / `libsqlite3-sys` 依赖删除，
  `app/src/schema.rs`（4850 行 DDL 拼装与 SQL 词法判断）整体作废，
  `schema.runQuery` 这条 IPC 与 SQL 编辑器相关资源（`sql.css`、`legacy/sql.js`）一并删除。
  现在全仓库（源码与 UI）搜不到任何 SQL 语句。
- 顺带消除了一个已知构建坑：bundled 的 `sqlite3.c`（约 9 MB 单体 C 文件）
  在本机 MSVC 上开优化会把 `cl.exe` 搞崩，原先得为它单独关 `opt-level`；现在没了。
- `db.rs` 里的结构迁移框架（`user_version` / `VACUUM INTO` 升级备份）删除 ——
  新引擎的表结构就是 JSON，字段增删是模型层面的事，不需要 `ALTER TABLE`。

### 新增

- **自研单文件存储引擎** `app/src/store/`：append-only 日志 + 快照，
  CRC32 校验 + `fsync` + 原子 rename。数据文件由 `main.db`（SQLite）变为 **`main.dkb`**。
  崩溃安全不靠"我觉得没事"，靠三条可验证机制：半写条目按 CRC 丢弃、提交即落盘、快照原子替换。
- **数据表模型** `app/src/model/`：表 / 字段 / 记录 / 视图，全部无查询语言。
- **关联字段（Link）**：一条记录指向另一张表的一条或多条记录，取代 `JOIN`。
- **共通字段（Shared Field）**：一次定义、多表引用；改一处，所有引用它的字段一起变，
  界面打 `⇄ 共通` 徽标 —— 这就是「知道这个数据是共通可以更新的」。
- **同步规则（Sync Rule）**：改一张表，关联表按规则一起更新。
  规则可开关、可改方向（镜像 / 双向 / 建议）、可配冲突策略与写入范围，
  目标字段自动打上来源标记 —— 这就是「可以调整」。
- **命名视图（View）**：筛选 / 排序 / 分组 / 隐藏列，取代 `SELECT`。**不提供任何查询输入框。**
- **界面入口**：数据页侧栏新增「关系与同步」（共通字段 + 同步规则的开/关/新建/删除）
  与「视图」（保存 / 应用 / 删除命名视图）两个入口 —— 引擎里有而界面够不着，等于没做。
- 存储层自检 `store::check_log`，给崩溃恢复向导用（取代 `PRAGMA quick_check`）。

### 变更

- 备份：由 `VACUUM INTO` 改为「压实快照 → 复制 → 三步校验」，扩展名 `.db` → `.dkb`。
- 崩溃恢复：一致性判据由 `quick_check` / `wal_checkpoint` 改为「日志能否被完整解析」，
  尾部半写被认成**可恢复**（并明确告知用户有一步没存上），而不是损坏。
- 笔记 / AI 设置 / 更新器设置 / 工作区状态：由 `sys_meta` 表改为自研存储的键值项。
- 路线图重排（M2 改为「多表与关联」，新增 M2.5 去 SQL 重构），`docs/06` 重写为
  《数据表引擎设计（无 SQL）》。

### 迁移

- **旧版 SQLite 数据文件不再被直接读取。** 迁移路径为「旧版导出 JSON / CSV → 新版导入」，
  因此主程序的依赖树里不需要保留 SQLite。`0.x` 期间不承诺向后兼容。

### 测试

- 新增 store 14 项 + model 18 项测试（含崩溃半写、快照截断、环检测、共通字段传播、命名视图）。
- 全量 252 项通过，0 失败。

---
## [1.4.0] - 2026-09-25 · 富文本笔记 + 让 AI 真的能干活（Agent 命令行接口）

### 新增

- **富文本笔记**。笔记编辑区多了 Markdown 工具栏（粗体 / 斜体 / 标题 / 列表 /
  引用 / 代码 / 链接）和「预览」切换，导出的网页也是渲染后的富文本。

  **为什么是 Markdown 而不是所见即所得**：所见即所得要动存储格式（正文现在是纯文本），
  那是另一个量级的工程。Markdown 是事实标准，写起来快，导出/迁移都不丢格式。
  渲染器**自己写的、零依赖**（引一个 marked/markdown-it 就是几十 KB）——
  自写最大的好处是**转义顺序由我们定**：先转义再套规则，用户写 `<img onerror=…>`
  不会变成真的标签（烟测专门验了这一条）。

- **Agent 命令行接口**（`deskbase agent …`）—— **这是"让 AI 真的能干活"的关键**。
  光给 AI 一份说明，它只能嘴上指导；要"导入导出、在应用里改数据"，就得给它
  一个能执行东西的入口。

  ```
  deskbase agent context                     # 应用上下文（给 AI 读）
  deskbase agent list-tables                 # 列出所有表（JSON）
  deskbase agent get-table --name X          # 列名与数据
  deskbase agent export --table X --out F    # 导出 CSV
  deskbase agent import --file F --table X   # 导入 CSV（表不存在就建）
  deskbase agent add-row --table X --data '{...}'
  deskbase agent update-cell --table X --rowid 1 --column C --value V
  deskbase agent delete-row --table X --rowid 1
  ```

  **为什么是命令行而不是本地 HTTP**：本项目"网络默认关闭"是红线，起一个监听端口
  （哪怕是回环）也和红线拧着，还带出鉴权问题。命令行没这个负担，任何 AI 工具
  （终端、脚本、MCP 的 command 通道）都能直接调。

  实测过一个完整往返：导入 CSV（2 行）→ 列出 → 加一行 → 改一格（100→120）→
  导出（3 行，改动在里头）。**所有写命令都进变更历史，界面里能回退。**

### 修复

- **「切回 never 后按钮重新禁用」是个真 bug，我上一轮误判了**。
  上一轮我放宽了等待并写进 D-070 说"重跑即过、不是 bug" —— **那个判断是错的**。
  真因：change 处理器里 `loadUpdateSettings()` 写在 `try` 块内，前面任何一步抛错
  （哪怕只是一次 IPC 超时）就会**跳过它**，于是出现"后端已经是 never、按钮却还点得动"。
  等待再久也等不到一个被跳过的调用 —— 这次把它挪到 `finally` 里，无论保存成功与否
  都把界面刷回后端真实状态。

### 验证

| 项 | 结果 |
|---|---|
| 单元 | **265 通过 / 0 失败** |
| 界面烟测 | **105 / 105**（新增富文本 5 步：工具栏 / 预览 / 渲染成标签 / 转义 / 切回）|
| Agent CLI | 完整往返实测通过（导入→列出→加行→改格→导出）|
| 门禁 | 五道全过 |

### 这一版没做完的（如实说）

| 事项 | 说明 |
|------|------|
| **AI 厂商接入 + 图标 + 模型识别** | 你提的三点我都记下了，但**这一轮没做** —— 它触及
| | 两条红线（"网络默认关闭"与"AI 默认本地"/远程要 E2EE），需要你先拍板
| | 云端厂商是"默认关闭+显式启用"还是干脆只做本地。下一轮按你的决定做 |
| **详细教程** | 要求"非常详细、且不能堆在设置里"（要单独界面 + 设置里放按钮）——
| | 内容量大，下一轮单独做 |
| 分词与倒排索引 | 现在能"快"，还做不到"任意子串秒搜" |
| 日程 / 最近项目 / 待办聚合 | 标 MVP 但**待拍板** |

---
## [1.3.0] - 2026-09-25 · 索引管理：搜内容不再全表扫描

### 新增

- **索引管理**。可以给表的某一列建索引，建了之后**搜这一列不再逐行扫** ——
  扫的是**去重后的值**。"客户"这一列 10 万行可能只有几千个不同的名字，
  扫几千个唯一值比扫 10 万行快一个数量级。

  表结构对话框里多了一块「索引」：选列 → 建索引；已有索引可删。
  建了索引的列，全局搜索走索引；没建的表退回限扫（保留 v1.2.0 的行为）。

  **几个刻意的选择**：

  | 取舍 | 为什么 |
  |------|--------|
  | 索引**定义持久化**、**内容只放内存** | 数据是会变的，内容落盘就得跟着每次写一起维护；
  | | 代价是重开要重建，**好处是永远不会有"索引和数据对不上"这种错** |
  | 写操作后**整个作废**、不做增量维护 | 增量维护一旦漏掉一种写路径，索引就会**静默地**
  | | 和数据不一致 —— 那种错的现象只是"偶尔搜不到"，极难查 |
  | 索引键存**原始值** | 存小写的话，显示出来英文会全变小写。大小写不敏感在比较时做 |

> 这是**朴素但真实**的索引，不是摆设：Rust 测试直接断言 `distinct` 明显小于
> `scanned_rows`，也就是"行数真的被压成了唯一值数"。
> 将来要"任意子串秒搜"再上分词与倒排，这个结构也不浪费（可以只换内层）。

### 验证

| 层 | 覆盖 |
|---|---|
| 索引生效（去重）| Rust：3 行 2 个不同值 → `distinct=2`，且同值的两行都挂在同一个键上 |
| 写后跟着变 | Rust：插入新值、改单元格、删索引，三种都断言过 |
| IPC 接线 + 异常路径 | 烟测 4 步（建/列/幂等重建/不存在的列要拦/删）|
| 界面 | 表结构对话框里的索引区，烟测未直接覆盖（需要手测）|

| 总计 | 单元 **265 通过 / 0 失败** · 烟测 **100/100** · 门禁五道 |

### 已知现象（不是缺陷，但值得知道）

- **新构建后第一次跑烟测会偶发超时**（`app.updateState` 请求超时）。原因是 exe 首次
  启动慢（磁盘缓存冷 / 系统安全扫描），重跑即过。v1.2.0 与 v1.3.0 各遇到过一次。

### 未做（继续如实列出）

| 事项 | 卡在哪 |
|------|--------|
| **富文本编辑** | 要动存储格式（笔记正文现在是纯文本），另一个量级 |
| 分词与倒排索引 | 现在能"快"，但还做不到"任意子串秒搜" |
| 日程 / 最近项目 / 待办聚合 | 标 MVP 但**待拍板**：定位改成"三件套扩展"后，
| | 这类 PIM 功能与定位的关系要确认 |
| 原生 PDF / docx | 网页导出已覆盖"打印"这个主要目的 |

---
## [1.2.0] - 2026-09-25 · 搜索能搜到表里的内容了

### 新增

- **全局搜索可以连表里的**内容**一起搜**（默认关，勾了才扫）。v1.1.0 只搜表名与
  笔记标题，现在勾上「连表里的**内容**一起搜」会逐行扫，命中会显示
  **是哪张表的哪一列、值是什么**。

  **为什么默认关、且每表只扫 2000 行**：引擎里还**没有数据索引**
  （`BTreeMap` 是记录存储，不是索引），所以这只能全表扫描 ——
  库大时会慢。三道闸：默认不扫 / 每表 2000 行 / 结果照样限量。
  界面上把这些如实写出来了，不让人以为"搜不到是坏了"。

### 修复

- 烟测里「切回 never 后按钮重新禁用」**偶发失败**（重跑能过）。原因：change 处理器
  要先落库再刷新按钮状态，是异步的，5 秒偶发不够。已放宽到 8 秒并给它启动时间 ——
  **会闪红的测试会被人习惯性无视**，宁可慢一点也要稳。

### 未做（继续如实列出）

| 事项 | 卡在哪 |
|------|--------|
| **真·索引管理** | 这是"又快又能搜内容"的正解。现在靠限量扫描顶着，
| | 引擎里的 BTreeMap 是记录存储，需要另建索引结构并持久化 |
| **富文本编辑** | 要动存储格式（笔记正文现在是纯文本），另一个量级 |
| 日程 / 最近项目 / 待办聚合 | 标 MVP 但**待拍板**：定位改成"三件套扩展"后，
| | 这类 PIM 功能与定位的关系要确认 |
| 原生 PDF / docx | 网页导出已覆盖"打印"这个主要目的 |

### 验证

| 项 | 结果 |
|---|---|
| 单元 | **263 通过 / 0 失败** |
| 界面烟测 | **96 / 96**（连跑两次确认稳定）|
| 门禁 | 五道全过 |

---
## [1.1.0] - 2026-09-25 · 补上「全局搜索」，工作台能用，笔记能导出网页

### 新增

- **全局搜索**（补 MVP 欠账）。功能矩阵把「全局搜索」标成 **MVP**
  （定义是"缺了它，产品无法成立或承诺被打破"），而代码里此前 **0 处实现**。
  工作台顶部现在有搜索框，搜**表名（含说明）与笔记的标题和开头**；
  结果可以直接点进去。

  > **刻意不扫表里每一行**：那是全表遍历，库一大界面就卡住。
  > 真要"找某一格内容"，得先做索引（见下方未做清单）。

- **工作台（首页）不再是空的**。以前那一屏只有一句"尚未实现"，现在有：
  - 搜索（上一条）
  - **状态**：几张表 · 多少行 · 几篇笔记 · 占了多大 · **上次备份是什么时候**

  > 状态里那条备份信息**不是装饰**：它对应「不丢数据」这条承诺 ——
  > 看不见的备份等于没有备份。**没备份过会明说**，不留空让人误以为备份了。

- **笔记可导出成网页（Ctrl+P 即存成 PDF）**。文档方向因此闭环了一层：
  能写、能导出 .md、现在也能打印/分享。

  > 为什么是 HTML 而不是直接生成 PDF：真做 PDF 要引排版与字体嵌入的依赖，
  > 体积和复杂度都不划算；而**用户的目的**是"能打印、能发给别人看"，
  > 一个打印友好的 HTML 在浏览器里 Ctrl+P 就是 PDF —— 目的一样，代价小得多。
  > 导出时做了 **HTML 转义**：笔记里写 `<script>` 不会把导出的页面变成可执行的。

### 未做（如实列出，不因为发了版就不说）

| 事项 | 为什么还没做 |
|------|------------|
| **富文本编辑** | 要动存储格式（现在的笔记正文是纯文本），是另一个量级的工程，
| | 应等文档方向的模板/导出站稳之后再做 |
| **索引管理** | 引擎里有 BTreeMap，但没暴露给用户。**这正是"搜表里某一格内容"的**
| | **前置** —— 有了它，全局搜索才能扩到单元格级别 |
| 日程预览 / 最近项目 / 待办聚合 | 功能矩阵标 MVP，但**待拍板**：ADR-0023 把定位改成
| | "Office 三件套扩展"后，这类**个人信息管理（PIM）**功能与定位的关系需要确认。
| | 要么补做，要么正式从功能矩阵里删掉并写明理由 —— 不能悄悄留着 |
| 导出 PDF（原生）/ docx | 网页导出已覆盖"打印"这个主要目的；docx 需求待确认 |

> 完整盘点见 `local-docs/reference/28-new-features-and-debt.md`。

### 验证

| 项 | 结果 |
|---|---|
| 单元 | **263 通过 / 0 失败** |
| 界面烟测 | **96 / 96**（新增工作台 4 步）|
| 门禁 | 五道全过 |

---
## [1.0.0] - 2026-09-25 · 第一个正式大版本

**v1.0 门槛逐条达标**（`VERSION_PLAN §3.5`，每条可查）：

| # | 门槛 | 依据 |
|:-:|------|------|
| ① | M0–M4 + M8 | M3 变更历史 · M1 文档方向起步 · **M4 可复现构建（本次实测）** · **M8 无障碍（本次机器体检 0 处）** |
| ② | 三条长期承诺 | 不丢数据（崩溃 16/16 + 备份三步校验）· 本地优先 · 零污染（便携版零注册表 + **安装版实测残留 0**）|
| ③ | Windows 内一致 | 便携版与安装版是**同一份 exe**，装不装由用户点按钮决定 |
| ④ | 文档完备 | **新增机器核对**（`check-docs.cjs`，进 CI）—— 另修掉 README 里 8 处与实现不符的地方 |
| ⑤ | 等效完整性链路 | SHA-256 · SBOM · 自替换默认关闭 · SmartScreen 说明 |

### 新增

- **可复现构建**。实测发现原来**不可复现**：同一份源码连做两次 release 构建，exe 的
  SHA-256 不一样（MSVC 链接器默认把时间戳写进 PE 头）。加上 `link-arg=/Brepro` 之后，
  两次构建（中间刻意 `touch` 源文件强制重编）哈希**完全一致**。
  没有签名证书的项目里，这是"你下载到的确实是官方构建的那一份"唯一不依赖信任的证明。
- **无障碍体检**（进走查）：页面内查四类"屏幕阅读器和键盘用户会直接卡住"的问题。
  首次运行抓到 3 处真问题 —— 笔记标题 / 正文 / 模板下拉**只有 placeholder**，
  而 **placeholder 不能当标签**（屏幕阅读器读不到，且一输入就消失）。修完归零。
- **文档一致性机器核对**（`scripts/check-docs.cjs`，门禁第五道，进 CI）：
  版本号自洽 / 路径引用存在 / 门禁脚本存在 / 反向提示"README 说没做但代码里有"。
  首次运行抓到 **4 处坏链接**，其中两处是**对外文档引用了 `local-docs/`** —— 读者根本看不到那个文件。

### 修复

- **README 与实现严重不一致**（8 处）：写着"安装版还没做""自动更新是手动下载"，
  而**两件事都早就做完了**；测试数停在 172（实际 263）、版本停在 v0.4.0。
  用户照着 README 会得到完全错误的印象 —— 这类问题人眼核对没用，**改完代码那一刻它就开始过期**，
  所以做成了机器核对。
- ADR-0020 引用的 ADR-0003 文件名不对（实际是 `0003-single-file-embedded-database.md`），
  并补上后记：它声称的"存储层保留"已被 ADR-0021 取代。

### 门槛修订（消除矛盾，不是放水）

- 撤销 v1.0 门槛第 ① 条里的"**缺加密**""**缺多语言**"两条。它们与 `VERSION_PLAN §2`
  （"加密 v1.0 前不做"）和 `docs/16`（"v0.1.0 只出中文界面 zh-CN"）**直接矛盾** ——
  门槛里列一条自己早就决定不做的事，会让 v1.0 在定义上永远达不到。
  这与 ADR-0024 当初修订的是同一类问题。**三条长期承诺一条没动。**

### 已知局限（一起说，别只看上面）

- **便携 zip 仍不可复现**：ZIP 把每个文件的 mtime 写进头部。里面的 exe 与 SBOM 是确定的，
  不确定的只是容器元数据。边界写在 `docs/20-reproducible-build.md`。
- **没有代码签名**：SmartScreen 会提示"未知发布者"。
- **校验链的固有局限**：SHA-256 与文件清单来自同一个 Release —— **账号一旦被攻破会一起失效**。
- **人工交互验收仍未做**：烟测 91 步 ≠ 人看着好用。

### 验证

| 项 | 结果 |
|---|---|
| 单元 | **263 通过 / 0 失败**（另 4 项联网与安装，需 `-- --ignored`）|
| 界面烟测 | **91 / 91** |
| 界面走查 | 7/7 步 · 4 档宽度 × 4 页 · **布局 0 处 · 无障碍 0 处** |
| 门禁 | **五道全过**（动效 / 对比度 / 接线 / 体积 / 文档）|
| 可复现性 | 两次构建哈希一致（`84382f91…`）|

---
## [0.12.0] - 2026-09-25 · 旧库迁移闭环：能看见，也能搬过来

### 新增

- **一键导入旧库**。上一版做到"能看见里面有什么"，这一版做到"能搬过来" ——
  迁移向导里每张表旁边多一个「导入到新版」，点一下整张表进新库。
  两件事差着用户的全部目的：看见只是确认没丢，搬过来才是接着用。

  几个刻意的处理：
  - **按旧库的声明类型决定新表列类型**（INTEGER→整数、REAL→小数、其余→文本）。
    金额列若一律按文本存，"按分存"的语义就没了，之后排序和计算都会出问题 ——
    导入的意义是"接着用"，不是"存下来看看"
  - **旧文件一个字节都不改**（界面上写明了）
  - 名字撞了**拦下来让你改名**，绝不覆盖你现在的表
  - 二进制字段存成「<二进制 N 字节>」占位而**不丢整行** ——
    丢行会让"导入了多少条"和旧库对不上，用户会以为丢数据了

### 验证（覆盖层级）

| 层 | 覆盖 |
|---|---|
| **导入链路（读→建表→写行→读回）** | Rust 测试：断言值真的进了新库，且整数列的类型映射生效 |
| **IPC 接线** | 烟测 2 步（scan / import 都能应答，报错可读） |
| **界面按钮** | ☐ 未覆盖 —— 需要有真实旧库的环境手测 |

| 总计 | 单元 **263 全绿** · 烟测 **91/91** · 四项门禁 |

---
## [0.11.0] - 2026-09-25 · 旧库能直接读了

### 新增：只读解析旧版 SQLite 数据库

旧库（v0.4.0 之前是 SQLite）换引擎后读不了。原来的说法是"用旧版程序导出成
Excel 再导入"—— 但**用户很可能已经没有旧版程序了**（卸载了、换机了），
只剩一个 `main.db`。那句指引等于帮不上忙，而里面是他唯一的数据。

现在迁移向导会**直接把它读开**，列出里面有哪些表、每张表有哪些列、前两行长什么样。
对"我的数据还在吗"这个最要紧的问题，这是最直接的答案。

**为什么自己写解析器，而不是引 `rusqlite`**：

1. ADR-0021/0022 刚把 SQL 依赖从项目里去掉（体积 + 供应链），加回来是倒退；
2. 我们只需要"**读**"这一件事，而且只读**我们自己的**旧库（结构简单）；
3. 红线 R2 要求新增依赖在 `DECISIONS_LOG` 写理由 —— 这个理由写不漂亮。

支持范围（**只读**）：表 B-tree、记录解码、溢出页、UTF-8/UTF-16 文本。
不支持：索引、视图、触发器、WITHOUT ROWID 表、加密库。
**遇到不支持的地方明确报错，不猜** —— 这是用户唯一的数据，猜错比不猜更糟。

测试用的夹具是**按 SQLite 真实格式手工拼的字节**（页头 / 单元指针 / 记录 /
varint / 各类 serial type），不是"看起来像"的假数据 —— 拼的过程本身
就是对照格式的复核。

> 「一键导入」还没接上，界面上如实写明，不让人以为点一下就能导。

### 验证（覆盖层级）

| 层 | 覆盖 |
|---|---|
| **解析器正确性** | Rust 6 个测试：varint / 建表语句取列名 / 能读表清单 / **能读出行和值** / 文件头不对要报错 / 页数对不上要报错 |
| **IPC 接线** | 烟测 1 步（没有旧库时如实返回 `found=false`） |
| **界面渲染** | ☐ 未覆盖 —— 需要有真实旧库的环境手测 |

| 总计 | 单元 **262 全绿** · 烟测 **90/90** · 走查 7/7 · 四项门禁 |

---
## [0.10.0] - 2026-09-25 · 关联字段终于有了界面入口

### 新增

- **关联字段能在界面上建了**。「关联」在引擎里就是一个列 + `link` 属性
  （列里存目标行的行号），引擎早就支持、19 个测试也都绿 —— 但**界面上一直没有入口**，
  等于两张表连不起来。"加列"表单里现在多了「列角色」：普通列 / 关联到另一张表；
  选了关联之后给「目标表」和「可以关联多条」两个选项
- **表结构里能看出关联关系**：关联列会标出「→ 目标表（可多条）」，
  共通字段标「⇄ 共通」。`ColumnMeta.link` 的注释本来就写着"界面要显示"，
  但界面一直没用它 —— 只显示列名和类型，等于没告诉人这列是干什么的

  > 反向字段（`back_field`）**没有**放进界面：引擎里目前没有处理它的逻辑。
  > 摆一个不生效的选项比不摆更坏。

### 修复（数据一致性缺口）

- **`add_column` 漏了"关联目标必须存在"的校验**。这条校验 `create_table` 里一直有，
  加列时却没有 —— 于是"不能建指向不存在表的关联"这条约束，在加列这条路上
  形同不存在。**而加列才是更常用的入口**（建表时得先有目标表，加列时两边往往都已存在）。
  实测确认：能通过加列建出关联到「根本不存在的表」的列，之后读到的是悬空引用。
  现在两条入口共用同一份校验，不会再分叉。

### 工具

- **烟测的等待条件太弱**：原来等的是"元素存在"，而 `<dialog>` 在构建那一刻
  就进了 DOM（内容还要异步加载）—— 早读会读到一个空壳，断言变成假红灯。
  改成等"内容真的就绪"（`.open`、清单有子元素）。
  实测踩到两次：历史对话框只读到"…关闭"两个字，列角色则是"根本找不到"。

### 验证

| 项 | 结果 |
|---|---|
| 单元 | **256 全绿**（+1：加列关联不存在的表要被拦下） |
| 界面烟测 | **89 / 89**（+5：编程链路 + **真对话框里点「列角色」**） |
| 界面走查 | 7/7 步 · 4 档宽度 × 4 页 · 布局 0 处 |
| 门禁 | 接线 / 动效 / 对比度 / 体积 全过 |

---
## [0.9.0] - 2026-09-24 · 界面打磨：先把"体检工具"修对，再改界面

### 新增

- **设置页分区导航**：8 张卡片以前只能一路滚（想改"外观"要先越过一整屏 AI 设置，
  找"教程"更是要滚到底）。现在顶部有一条吸顶的锚点条，点击平滑滚过去。
  锚点从卡片标题**自动生成**，不手写清单 —— 将来加卡片不用记得改两处。

### 修复（都是真问题，附实测数据）

| 问题 | 数据 |
|------|------|
| 表格页窄屏抽屉**没关干净** | 640 宽下右边缘露出 **92px**，用户看到半截按钮 |
| 笔记页编辑区在 800 宽**溢出** | 右边界到 833（视口 800）—— 上一版加动作按钮时引入的 |
| 表格页侧栏**行数对不上** | 声明 4 行却放了 10 个子元素，那个 `1fr` 落到 |
|  | "从 Excel 导入"按钮上把它拉长当留白；列表内容一变高就塌 |
| 空态引导块**居中且溢出** | 它被塞进 `.db-empty`（那个类是给"一行提示"设计的， |
|  | `text-align: center`），正文画到右侧表格区上、越界约 68px |

### 工具改进：界面走查体原来在"量寂寞"

这轮最有价值的其实是这件事 —— **体检报"0 处问题"是假的**，它一直在量隐藏元素：

- **切页后立刻体检**：视图还没激活（`.view` 非 active 时 `display: none`），
  量到的每个元素 rect 都是 0，于是"全过"。现在先等视图可见，再等异步内容落地
  （表格列表是懒加载 + 引导块靠 MutationObserver 插，固定等 320ms 都拍不到）
- **"部分露出"判据用错**：原本按几何位置判断，而 `getBoundingClientRect`
  返回的是**布局位置**，祖先 `overflow: hidden` 裁掉了它照样往外报 —— 40 条误报
  把真问题埋了。改用"那一块能不能真点到"（`elementFromPoint`）
- **新增两条规则**：窄屏下点击目标过小（并区分"被 label 包着的原生控件"，
  它的可点区域是整个 label）；文字**画到盒子外面**（元素 rect 查不出，得用 Range 量）

### 验证

| 项 | 结果 |
|---|---|
| 单元 | **255 全绿** |
| 界面烟测 | **80 / 80**（新增分区导航 3 步） |
| 界面走查 | 7/7 步 · 4 档宽度 × 4 页 |
| 门禁 | 接线 / 动效 / 对比度 / 体积 全过 |

---
## [0.8.0] - 2026-09-24 · 安装版可用了，文档方向开了个头

### 新增

- **安装版终于能用**（长期卡住的那条）。原来的死结是：本机把 `reg.exe`（注册表
  命令行工具）列进了程序黑名单，进程一起来就被杀，于是写不了「应用和功能」的条目。
  **但注册表本身可以直接用 Win32 API 读写** —— 命令行工具只是个壳，绕开壳不影响能力。
  现在：
  - 设置页新增「安装到本机」：**用户级安装**（`HKCU` + `%LOCALAPPDATA%\Programs`），
    **不需要管理员**，不碰 `HKLM`
  - Windows「应用和功能」里会出现一条记录，可正常卸载
  - **便携版行为完全不变**：不点安装就一个字节都不写注册表
  - 卸载会清掉注册表项与程序文件，**保留用户的数据目录**（卸载程序不该替用户
    决定他的报表要不要留）。正在运行的那个 exe 删不掉自己，如实标记为"重启后清除"
    —— 不假装删干净了
- **笔记模板与导出**（文档方向的第一步）：
  - 5 个常用骨架：会议纪要 / 工作日报 / 工作周报 / 待办清单 / 读书笔记
  - **导出为 Markdown**：写好的东西能带走了。此前笔记只能存、不能出
  - 为什么先做这两件而不是富文本：模板解决"打开一片空白不知从哪写起"，
    导出解决"写完了带不走"；富文本要动存储格式，得等这两件站住

### 修复

- **一条偶发测试**（`backup::备份_同秒重复调用不覆盖`）：它假设"两次调用落在
  同一秒"，而时间戳取自真实时钟 —— 机器稍卡就跨秒变红，看起来像功能坏了。
  已拆出 `create_named`（文件名由调用方给），测试改为确定性地构造"同名"，
  不再赌时序。**偶发通过的测试等于没有测试。**

### 验证

| 项 | 结果 |
|---|---|
| 单元 | **255 全绿** |
| **安装端到端**（真装一次再真卸一次） | **通过** · 卸载残留 **0** |
| 界面烟测 | **77 / 77**（新增安装 4 步 + 笔记 3 步） |
| 门禁 | 接线 / 动效 / 对比度 三项全过 |
| 产物 | 4.89 MB · 编译零警告 |

---
## [0.7.0] - 2026-09-24 · 旧库迁移向导，并把自动更新查了个底

### 新增

- **旧版数据文件的迁移向导**。上一版只给一句提示，现在检测到旧 `main.db` 会
  弹一个对话框：**三步迁移指引 + 直接打开数据目录** —— 让人立刻能动手，
  而不是先去猜目录在哪。旧库换引擎后读不了也删不掉，最容易被当成"数据丢了"，
  这件事不能只靠一句话打发。

### 检查（结论：自动更新是通的）

之前的交接文档一直写着"网络传输尚未实现"。**实测下来不是这样** —— WinHTTP
那段已经落地，只是没人真跑过。本版补了一条真联网测试后确认：

| 联网测试 | 结果 |
|---|---|
| `tests_net::联网检查更新能拿到清单` | ✅ |
| `tests_release::真联网能取到发布列表`（本版新增） | ✅ |
| `tests_e2e::端到端下载并暂存`（**真的下载了**） | ✅ |

跑法：`cargo test -- --ignored`（这三个依赖网络，不进常规测试）。

### 变更（承接上一版）

- **变更历史与回退**：每次改动留一条记录（**与数据变更同批提交**，要么都记
  要么都不记），表格页侧栏「历史」入口可逐条回退。两个刻意的限制：只记改与删
  （新增不会丢东西）、每表上限 500 条（是"改错了能回退"，不是全量审计）
- **构建链**：MSVC 探测补上非标准安装位置（本机 VS 在 `E:\vs2026`，原探测扫不到）；
  vcvars64.bat 跑不出来时改为自行构造 PATH / LIB / INCLUDE，不再直接卡死

### 验证

| 项 | 结果 |
|---|---|
| 单元 | **255 全绿**（+3 个联网测试，需 `--ignored`） |
| 界面烟测 | **70 / 70** |
| 门禁 | 接线 / 动效 / 对比度 / 体积 四项全过 |
| 产物 | 4.87 MB · 编译零警告 |

---

### 新增

- **变更历史与回退**（"改错了能回退"）。项目承诺「不丢数据」，
  但此前兑现的只有"崩溃不丢"那一半 —— 误删一行、误改一格只能看着。
  现在每次改动都会留一条记录（**与数据变更同批提交**，要么都记要么都不记），
  表格页侧栏新增「历史」入口，可以逐条退回去。

  设计上刻意做了两个限制，理由都写在代码注释里：

  - **只记「改」与「删」** —— 新增不会丢东西，记进去只会让列表变吵
  - **每表最多 500 条**（超出丢最旧的）—— 它是"改错了能回退"，
    不是全量审计；不设上限数据文件会无限长

### 修复

- **构建链：MSVC 探测漏掉了装在非标准位置的 VS。** 本机 VS 在 `E:\vs2026`，
  既不在 Program Files 下、目录名也不是常见形式，原来的探测扫不到 —— 现在
  会把每个盘符根下的 `vs*` / `VS*` / `VisualStudio*` 都收进候选。
- **构建链：vcvars64.bat 跑不出来时不再直接报错。** 它内部要调 `reg.exe`
  查 KitsRoot10，而 reg.exe 在受限环境里会被直接杀掉（退出码 null、零输出）。
  现在这种情况会改用**自行构造**的 PATH / LIB / INCLUDE —— MSVC 工具链与
  SDK 目录都是可直接探测的，不必经过注册表。
- `build.cjs` 在直接启动 cargo 被拦时（退出码 null）改走 PowerShell 通道。

### 验证

- 单元 **255 全绿**（新增 3 个：改一格能退回 / 删一行能整行恢复 / 历史有上限）
- 界面烟测 **70 / 70**（新增 2 步：历史入口存在、点下去界面有反馈）

---
## [0.6.0] - 2026-09-23 · 裁决 v1.0 门槛矛盾，并补上端到端验收

### 新增

- **ADR-0024：修订 v1.0.0 的门槛**（Q-045 的正式裁决）。原门槛有**三条**与已生效的决定
  直接矛盾，导致 v1.0.0 在定义上永远达不到 —— 现在按下面三条修订，每条仍可验证：
  - ① M0–M8 → **M0–M4 + M8**，M5 本地 AI / M6 远程与移动 / M7 插件**移出** v1.0 判定范围
  - ③ 三平台一致 → **目标平台（Windows）内一致**（跨平台为 v1.0 之后的独立议题）
  - ⑤ 签名 → **签名或等效的完整性链路**（无证书时的五件必备，逐条列明）
- **端到端验收终于跑起来了**：`tests/e2e-import.cjs` **27 / 27**。此前一直缺真实表格夹具
  而从未执行；本版补上后一次通过，并修掉一处过时断言（分页有 500 行的硬上限，
  因此不能靠"一次取 2000 行"来数行 —— 改用 `schema.getTable` 的精确行数）。
- `tools/make-e2e-fixture.cjs`：可重复生成业务味的表格夹具（1200 行，含前导零工号、
  金额、日期、空值），不依赖第三方包。

### 变更

- **距 v1.0.0 的差距现在有了一张逐条可查的清单**，写在 `local-docs/handoff/VERSION_PLAN.md`
  §3.5 之后。结论是：**v1.0.0 现在是"可达"的（定义矛盾已解决），但尚未"已达"** ——
  还差安装版（`reg.exe` 被拉黑而未做）、版本历史、加密、可复现构建、无障碍与多语言。

### 验证

| 项 | 结果 |
|---|---|
| 单元 | 252 全绿 |
| 界面烟测 | 68 / 68 |
| **端到端验收** | **27 / 27**（此前从未跑过） |
| 导入压力测试 | 22 / 22（1 万 + 10 万） |
| 崩溃恢复 | 16 / 16 |
| 门禁 | 四项全过 · 编译警告 0 |

---
## [0.5.0] - 2026-09-22 · 定位转向 Office 三件套的扩展与升级

> 这是一次**产品定位的重定**，不是一个功能补丁。
> DeskBase 不再以「数据库」自居 —— 详见
> [ADR-0023](docs/adr/0023-office-suite-extension.md)。

### 变更

- **产品定位转向「Office 三件套的扩展与升级 + 办公实用功能集**（ADR-0023）。
  不替代 Office，补它不做与做不好的那块；三件套方向为文档 / 表格 / 演示，
  底座（本地优先、隐私优先、单文件、零遥测）不变。
- **一级模块改名**：「数据库」→ **表格**（去掉 SQL 之后这个词是误导），
  「工作台」→ **工具**。演示方向未开工，因此**没有**放进导航 ——
  放一个点不开的入口比不放更糟。
- **教程重写**：从「数据库使用教程」改为「办公套件使用教程」（9 节），
  补上 v0.4.0 的新能力（共通字段 / 同步规则 / 关联字段 / 命名视图）与隐私、备份说明。
- 路线图重排：M2.5 去 SQL 标记完成、M2 明确为「三件套之一：表格」、
  新增 M2.6（文档与演示方向）。
- 参考文档口径同步：`README.md`、`AGENTS.md`、`docs/03`。

### 新增

- **旧版数据文件提示**（`app.legacyDb`）：升级后若旧的 `main.db` 还在，启动时
  明确告知「数据还在里面、没丢」并给出迁移路径。换引擎之后最容易发生的
  误会就是"我的数据没了" —— 这条不能等用户来问。
- 视图内**平滑滚动**（标准 / 丰富档位；「关」与「精简」档下不启用）。

### 修复

- **设置页滚不动**（连带造成"教程没写"的错觉）：设置页有 7 张卡片，教程挂在最后一张，
  滚不过去就永远够不到。两层原因 ——
  ① `.view` 没有 `overflow`；
  ② 根因在 `.main` 的 `grid-template-rows: auto 1fr`：网格行默认 `min-height:auto`，
     内容更高时行被撑开，`clientHeight` 等于内容高度（实测 5093），`overflow` 不触发。
     改成 `auto minmax(0, 1fr)` 后：`clientH` 5093 → 665，能滚到底。
- 界面自检不再检查已删除的 `sql` 模块（此前 app.log 恒报"模块未加载 sql"）
- 压力测试脚本去掉最后一处 `schema.runQuery`，改用 `schema.getTable` 的精确行数
- 崩溃恢复验收脚本的断言不再死盯 `quick_check`，改为判"日志能否被解析"

### 验证

| 项 | 结果 |
|---|---|
| 单元 | **252 全绿** |
| 界面烟测 | **68 / 68** |
| 界面走查 | **7 / 7** · 18 张截图 · 窄屏四档布局体检 **0 处**疑似 |
| 崩溃恢复端到端 | **16 / 16** |
| 导入压力测试 | **22 / 22**（1 万 + 10 万） |
| 门禁 | 动效 / 对比度 / 接线 / 体积 四项全过 |
| 编译警告 | **0** |

### 新增

- **旧版数据文件提示**（新增 IPC "app.legacyDb"）：升级到 v0.4.0 后若检测到旧数据文件
  main.db，启动时明确告知「数据还在里面、没丢」，并给出迁移路径
  （用旧版打开 → 导出 CSV/Excel → 用新版「从 Excel 导入」）。
  换掉存储引擎之后最容易发生的误会就是"我的数据没了" —— 这条必须主动说，不能等用户来问。

### 变更

- **产品定位转向「Office 三件套的扩展与升级 + 办公实用功能集**（ADR-0023）。
  不替代 Office，补它不做与做不好的那块；三件套方向为文档 / 表格 / 演示，底座仍是
  本地优先、隐私优先、单文件、零遥测。
- **教程重写**：从「数据库使用教程」改为「办公套件使用教程」（9 节），
  补上 v0.4.0 的新能力（共通字段 / 同步规则 / 关联字段 / 命名视图）与隐私、备份说明。
- 路线图重排：`ROADMAP.md` 北极星改写，M1 改为「办公台：记与截」，M2 明确为
  「三件套之一：表格」，M2.5 去 SQL 重构标记完成，新增 M2.6（文档与演示方向）。
- 参考文档口径同步：`README.md`、`AGENTS.md`、`docs/03`（一级模块按
  文档 / 表格 / 演示 / 工具 / 设置 组织）。

### 修复

- 界面自检不再检查已删除的 sql 模块（此前 app.log 会恒报"模块未加载 sql"）
- 压力测试脚本去掉最后一处 schema.runQuery，改用 schema.getTable 返回的**精确行数**
  （新引擎不用再给估计值）
- 崩溃恢复验收脚本的断言不再死盯 quick_check 等于 "ok"，改为判"日志能否被解析"

### 验证（v0.4.0 发布后补跑，针对新引擎）

- 崩溃恢复端到端 **16 / 16**：强杀 → 重启报「上次未正常退出」→ 自检通过 →
  已提交 150 行一行不丢 → 正常退出不再误报
- 导入压力测试 **22 / 22**（1 万 + 10 万两档）：10 万行导入 2.2 s（45,600 行/秒），
  分页 / 排序 / 筛选 / 值抽查全对
- 单元 252 全绿 · 烟测 61/61 · 三项门禁全过 · 编译警告 0

### 新增

- 初始化项目仓库与开源治理文件
  （`README.md`、`LICENSE`、`CONTRIBUTING.md`、`CODE_OF_CONDUCT.md`、`SECURITY.md`、`ROADMAP.md`、`GOVERNANCE.md`）
- 建立完整产品与工程方案文档体系（`docs/00` – `docs/17`）：
  - 总体方案摘要
  - 愿景、边界与核心原则
  - 目标用户与典型使用场景
  - 信息架构（一级模块 / 二级功能 / 三级设置）
  - 完整功能矩阵（MVP / 进阶 / 插件化）
  - 办公工具箱详细设计（含长截图、自定义快捷键、教程系统、设置界面）
  - SQL 数据库能力设计
  - 备份、恢复、断电保护与容灾设计
  - 隐私、安全、加密、权限与审计设计
  - 本地 AI 架构与隐私保护策略
  - 原生桌面端与手机远程端交互与同步架构
  - 安全与资源优化专项（安装、便携、卸载、注册表残留、后台进程、缓存与日志）
  - 安装界面、安装流程、卸载界面与卸载流程详细设计
  - 设置界面、教程系统、帮助系统详细设计
  - 性能优化清单与可量化指标
  - 开源治理、许可证、社区协作与插件生态建议
  - 分阶段路线图、风险、取舍与验收标准
  - 技术选型建议（候选方案 / 优缺点 / 适用条件，不绑定单一技术栈）
- 建立架构决策记录机制与初始 ADR
- 建立 Issue 模板、PR 模板与仓库协作约定
- 建立术语表，统一全项目名词
- 接受架构决策 **[ADR-0021 · 彻底移除 SQL，改用自研单文件存储引擎](docs/adr/0021-remove-sql-own-storage-engine.md)** 与 **[ADR-0022 · 多表关联与协同更新模型（可配置、可追溯）](docs/adr/0022-linked-tables-and-sync-rules.md)**（日期 2026-09-20，已接受）：
  - 彻底移除 `rusqlite` / `libsqlite3-sys`，代码与界面同时清零 SQL；
  - 用「共通字段 + 同步规则 + 关联 + 查找/汇总 + 命名视图」取代 `JOIN` / 外键级联 / 触发器 / `SELECT`，界面不提供任何查询输入框；
  - 数据文件由 `data/main.db`（SQLite）变为 `data/main.dkb`（自研 append-only 日志 + 快照 + CRC32 + fsync + 原子 rename）；旧库不直读，迁移走「旧版导出 JSON/CSV → 新版导入」；
  - 取代 ADR-0003 中「不自研存储引擎」一条，其余关于单文件 / 可带走的要求继续有效。
- 重写数据表设计文档：`docs/06-sql-database.md` → **`docs/06-data-table-engine.md`（无 SQL）**；同步更新 `README.md` / `ROADMAP.md` / `AGENTS.md` 中的去 SQL 表述与链接。
- 路线图重排（2026-09-20）：M2 主题改为「数据核心：多表与关联」，新增 **M2.5 · 去 SQL 重构**（自研存储引擎 ✅、移除 rusqlite 🚧、表/记录模型 🚧、关联与同步规则 📋、命名视图 📋、旧库迁移 📋、UI 改造 📋）；「明确的『不做』」新增「不再内置 SQL 引擎与 SQL 查询界面」「不提供通用数据库客户端能力」。

---


## [0.3.0-beta.11] - 2026-09-20

**主题：AI 使用记录（审计）。**

ADR-0017 要求 AI 调用（含被拒的）全部留痕。这一版把审计落地：

- **单独一份 `logs/ai-audit.log`**（JSON Lines）：与排障日志分开，
  用户想回答「AI 到底往外发过什么」时不用在流水里捞。

- **只记元信息**：时间 / 动作 / 厂商 / 表名 / 列名 / 行数 / 结果 / 说明。
  **结构里没有任何一格能装下一行数据** —— 这是设计出来的，不是碰巧。
  测试专门断言：审计文件里不得出现数据本体，也不得出现 Key。

- **被拒的也要记**（用户点取消同样是审计事件）。

- 设置页新增「查看使用记录」：显示最近 20 条的摘要（不展示任何数据内容）。

### 还没做（下一步）

**真正的调用通道**。它需要复用更新器的 WinHTTP 封装，而那个封装目前是私有的、
且只有「下载到文件」没有「POST 到内存」—— 改它意味着动到刚验证过的更新器
（联网测试 2/2 的那个）。**有回归风险，单开一轮做**：加一个不动现有路径的
`post_json`，再做两阶段授权（prepare 预览 → 用户确认 → run 发送）。

### 测试

- cargo test **291 / 291**（+2 审计）· 界面烟测 **55 / 55**

## [0.3.0-beta.10] - 2026-09-20

**修复：AI 设置卡读不到数据（下拉是空的）。**

上一版（beta.9）把 AI 卡片的接线写在了 `app.js` 主 IIFE **之外**，取不到内部的 `call`，
于是读厂商清单与设置全部静默失败 —— **界面看着正常，下拉里却一家厂商都没有**。
改成走 `window.__deskbase.call`（与 db.js 同一条桥）后正常。

这条是被**烟测抓住的**：新加的三步里"服务商下拉里有主流厂商（0 家）"直接变红。
这正是把"默认关闭"和"下拉有厂商"写成断言的价值 —— 少了它，这个缺陷会一路发出去。

### 测试

- cargo test **289 / 289** · 界面烟测 **53 / 53**

## [0.3.0-beta.9] - 2026-09-20

**主题：AI 表格的地基 —— 能配了（还不能发）。**

### 新增（P2 第一步 · 配置地基）

- **设置页多了「AI 表格」一张卡**：总开关（**默认关闭**）、**服务商下拉**（DeepSeek / 通义千问 /
  智谱 GLM / Kimi / 豆包 / OpenAI / Claude / Gemini / 自定义·本机 Ollama）、接口地址、
  模型名、你自己的 API Key。

- **厂商清单是配置数据**（`ai.rs` 的 `PROVIDERS` 一张表）：加一家只加一行，
  不散落在代码里 —— 也**不引入任何厂商 SDK**，全部走 OpenAI 兼容接口，
  所以"接本机 Ollama"和"接云厂商"是同一条路。

- **默认关闭且界面不做任何暗示**（ADR-0017 的"默认关闭"）：烟测专门钉住"AI 默认是关的"。

- Key **只存本机数据库**（`sys_meta`），**日志只记开关与厂商名，绝不记 Key**。
  设置页也如实写明这一点。

### 说明：这一版**还不能调用 AI**

按 ADR-0017，发数据行必须**逐次显式授权 + 展示将发送的内容**。授权闸门与审计是下一步，
本版先把"能配"落地 —— 配置不对就贸然加调用，等于把两道闸一起绕过去。

### 测试

- cargo test **289 / 289**（+3：默认关闭 / 存取回环 / 厂商清单含主流那几家）

## [0.3.0-beta.8] - 2026-09-20

**主题：把一列里的值整理干净 —— 只改值，不改类型，不重建表。**

### 新增（P1-5 · 值规范化）

- **列头「⋯」菜单里多了两项**：**统一日期格式** / **清洗数字**。
  - 日期：`2026/1/5`、`2026.1.5`、`2026年1月5日`、`２０２６-１-５` 统统变成 `2026-01-05`
  - 数字：去掉千分位、`¥`、`元`、空白，全角数字转半角（`¥1,234.50` → `1234.50`）
- **为什么是这条路**：渲染成什么样由**列的声明类型**决定，而 SQLite 改列类型
  **只能重建表**（M/L 规模）。但把值统一格式，**文本排序就正确了** ——
  同一个日期写成三种样子会让排序全乱，这才是真正扎人的地方。
- **三条原则**（都写进代码注释）：看不懂的值**原样保留并报出来**（不静默丢）；
  已经规范的值**不写盘**；**主键列不参与**（它可能被别的表引用）。
- 报告会告诉用户「改了几行、几行看不懂」，看不懂的前 5 条连行号一起写日志。

### 测试

- cargo test **286 / 286**（+5：日期各种写法 / 越界与看不懂 / 数字清洁 /
  整列跑一遍且跳过行被报出 / 主键列拒绝）

### 顺手记一个坑（写给自己和下一个 AI）

`schema.rs` 里**已经**有 `normalize_date`（返回值 → 存储格式，返回 `Result`）。
新加的"值规范化"用了同名函数，编译期直接炸 —— 已改名为 `iso_date` / `clean_number`。
**同一个模块里加新工具函数前，先 grep 一遍函数名。**

## [0.3.0-beta.7] - 2026-09-20

**主题：列头能点了 —— 表格手感补上最后一块。**

### 新增（P1 第 4 项后半 · 列头菜单）

- **鼠标移到列头会出现一个「⋯」**：点开就能**改列名 / 加一列 / 删掉这一列**。
  三个动作全部复用已有 IPC，界面层不新增业务规则 —— 三道 SQLite 暗礁
  （主键、必填要有默认值、被索引引用）依旧由 Rust 拦下并说人话。
- **分层没有破坏**：网格（grid.js）依旧不认识业务，它只把"用户点了第几列"
  抛给上层（`cfg.onColumnMenu`），由页面层（db.js）决定能做什么 —— 与
  `commitCell` / `deleteRows` 同一套约定。
- 「加一列」复用「表结构」底部的加列表单（一处实现，两处入口），不再写第二份。
- 烟测 +1 步（列头菜单编程入口）。

### 测试

- cargo test **281 / 281** · 界面烟测 **49 / 49** · 三闸全过

## [0.3.0-beta.6] - 2026-09-20

**主题：列名能改了 —— 补上上一版留下的缺口。**

beta.5 给新建表格预填了三个列名（列1/列2/列3），但建完就改不了 —— 这一版补上入口。

### 新增（P1 第 4 项前半）

- **改列名**：表结构对话框里每一列多了「改名」按钮。数据、索引、视图与注释**都跟着走**：
  SQLite 的 `RENAME COLUMN` 会自动更新引用这一列的索引 / 视图 / 触发器；
  注释表里的记录由我们搬过去（不搬就是挂在新列名上看不见、挂在旧列名上找不到的幽灵）。
- 三种错都拦下并说清：**重名**（已有叫「x」的列）、**同名**（和原列名一样）、
  **不存在**（表里没有这一列）。列名规则沿用 `validate_column_name`（禁 `rowid`、禁空格标点）。

### 测试

- cargo test **281 / 281**（+2：改列名后数据与注释跟随 / 冲突与保留名拦截）
- 界面烟测 **48 / 48**（+1：改列名后新名在、旧名没）

### 还没做（P1 第 4 项后半）

- **列头菜单**（Excel 式：直接点列头就加列 / 改名 / 删列）—— 本轮先把"能改"打通，
  入口放在已有的"表结构"对话框里；列头是虚拟渲染，改造面更大，单独一轮做。

## [0.3.0-beta.5] - 2026-09-20

**主题：新建表格不再要求你先想清楚「类型」。**

### 变更（P1 第 3 项 · 免选类型）

- **新建表格预填三个列名**（列1 / 列2 / 列3）：想改就在对话框里改，不想改直接点创建 ——
  不再被三个空输入框拦在门口。（Excel 的心智是"先有表，名字慢慢改"。）
- **类型下拉弱化**：默认仍是文本，加了提示"一般不用改：默认按文本存，什么内容都能装"。
- **主键勾选框加提示**"一般不用动"：它没被删 —— 它还决定某一行能否被编辑，
  删了会踩到教程里那条错误对照。
- 教程「三、列类型怎么选（9 种）」改为「三、列类型（先不用管）」，与实现对齐。
- 界面烟测 +4 步：对话框能开 / 默认三行 / 列名已预填 / 类型默认文本。

### 已知遗留（不藏）

- 新建对话框的说明文案仍是旧版（一次锚点没对上）—— 它描述的类型语义没错，
  但与"类型不用管"的新口径不完全一致，下次顺手改。
- **列建好后改名还没有入口**（本版只做到"建的时候能改名"）：
  计划在 P1 第 4 项「网格内加列 / 改列名 / 删列」一并解决。

### 测试

- cargo test **279 / 279** · 界面烟测 **47 / 47**

## [0.3.0-beta.4] - 2026-09-20

**主题：换形态 —— 从「数据库」到「表格」。**

按 ADR-0020（发起人决定）把产品形态改为 Excel 式表格优先，SQL 从产品移除。
**数据模型与存储都没动 —— 用户数据无需迁移，已有能力全部保留。**

### 变更

- **术语全部改写**（196 处）：库表→表格、建表/新建表→新建表格、字段→列
  （含"每个字段"→"每一列"这类通顺修正）。范围覆盖界面文案与教程，
  以及 Rust 侧用户可见的错误信息与日志。
- **SQL 编辑器撤下**：页签栏移除、资源表登记移除、`sql.js` 移到 `app/ui/legacy/`。
  策略层闸门与挂载代码**刻意保留** —— JS 无死代码告警，留着是零成本可逆点；
  恢复方式写在 `app/ui/legacy/README.md`。
- 教程「六、用 SQL 查」整节撤下，节号顺延（不再教用户用不存在的入口）。
- 空态文案改为"把 Excel / CSV 拖进来"（不再提 SQL 页签）。
- 跨语言命名门禁的 `ui_src` 加 legacy 回退：撤下的组件仍受同一条规则约束。

### 依据

- [ADR-0020 表格优先的界面形态与 AI 表格](docs/adr/0020-table-first-ui-and-ai-tables.md)
- 下一步：P1 第 3 项（新建表格免选类型 + 网格内加列改列）、P2（AI 表格，
  **内置主流厂商** + 逐次授权 + 审计）

### 测试

- cargo test **279 / 279** · 界面烟测 **43 / 43** · 三闸全过

## [0.3.0-beta.3] - 2026-09-19

**主题：表建完还能改；导入向导不再互相挤。**

### 新增 · 表结构编辑（v0.3.0 清单 F 包）

- **改表名**：数据、索引、注释一起跟着新名字走（注释单独存在元数据表里，
  不跟着搬就等于弄丢）；新名撞车会被挡下。
- **加字段**：沿用建表时的字段定义与九种类型；**必填不给默认值会被挡**
  （已有的行没东西可填）、**主键列不许补**（SQLite 只允许建表时定）。
- **删字段**：主键列与最后一个字段删不得；被索引 / 视图 / 触发器引用时
  SQLite 会拒绝 —— 这时会说清"常见原因是被引用"，而不是抛一句英文原文。
  被删字段的注释一并清掉（留着就是幽灵注释）。
- **改表注释**：顺手清掉了 set_table_comment 上的 allow(dead_code)。
- 界面烟测新增 7 步（入口 / 编程入口 / 加列 / 删列 / 改名 / 改名后列表一致 / 删主键列被拒）。

### 变更

- **产品定位转向「Office 三件套的扩展与升级 + 办公实用功能集**（ADR-0023）。
  不替代 Office，补它不做与做不好的那块；三件套方向为文档 / 表格 / 演示，底座仍是
  本地优先、隐私优先、单文件、零遥测。
- **教程重写**：从「数据库使用教程」改为「办公套件使用教程」（9 节），
  补上 v0.4.0 的新能力（共通字段 / 同步规则 / 关联字段 / 命名视图）与隐私、备份说明。
- 路线图重排：`ROADMAP.md` 北极星改写，M1 改为「办公台：记与截」，M2 明确为
  「三件套之一：表格」，M2.5 去 SQL 重构标记完成，新增 M2.6（文档与演示方向）。
- 参考文档口径同步：`README.md`、`AGENTS.md`、`docs/03`（一级模块按
  文档 / 表格 / 演示 / 工具 / 设置 组织）。

### 修复

- **导入会话上限只有 2**（KEEP_SESSIONS）：用户开到第三个导入向导时，
  第一个会被悄悄挤掉，界面只看到一句"导入已结束或过期"；测试并行跑时同样
  互相挤。这个上限的目的只是防泄漏，不是限制并发 —— 提高到 32。
  （2026-09-19 由新增测试改变并行时序后暴露。）

### 测试

- cargo test **279 / 279**（+7 表结构测试）
- 界面烟测 **44 / 44**（+7 表结构步骤）

## [0.3.0-beta.2] - 2026-09-19

**主题：备份按钮 + 自动更新在本机真的能用。**

beta.1 之后的第一个迭代。加上了「备份数据库」（三条底线里"不丢数据"的
主动兑现 —— 此前只有"升级库结构前的自动备份"），并把自动更新的网络兜底补齐。

### 新增

- **备份数据库（数据库页侧栏）**：一键生成完整快照。用 `VACUUM INTO` 而不是
  复制文件 —— 复制一个正在写入的 .db 可能拿到半截状态（WAL 还没落盘）。
  备份存到数据目录的 `backups/`（与迁移前自动备份同处），**生成后立刻三步校验**：
  非空 / 能独立打开 / `quick_check` 通过且表数与当前库一致。校验不过会明说原因 ——
  "看起来像备份"的空壳比没有备份更危险。列表显示每份的大小与时间。
- 界面烟测新增 6 步：备份入口、打开对话框、点「立即备份」、确认新备份出现、
  文件名约定（`deskbase-YYYYMMDD-HHMMSS.db`）、大小时间显示。

### 变更

- **产品定位转向「Office 三件套的扩展与升级 + 办公实用功能集**（ADR-0023）。
  不替代 Office，补它不做与做不好的那块；三件套方向为文档 / 表格 / 演示，底座仍是
  本地优先、隐私优先、单文件、零遥测。
- **教程重写**：从「数据库使用教程」改为「办公套件使用教程」（9 节），
  补上 v0.4.0 的新能力（共通字段 / 同步规则 / 关联字段 / 命名视图）与隐私、备份说明。
- 路线图重排：`ROADMAP.md` 北极星改写，M1 改为「办公台：记与截」，M2 明确为
  「三件套之一：表格」，M2.5 去 SQL 重构标记完成，新增 M2.6（文档与演示方向）。
- 参考文档口径同步：`README.md`、`AGENTS.md`、`docs/03`（一级模块按
  文档 / 表格 / 演示 / 工具 / 设置 组织）。

### 修复

- **检查更新在本机 5/5 全失败**（用户报告"自动更新必须能用"后排查）：根因是只有
  一条网络路径（跟着系统代理走），而本机的代理软件会进入"端口还在监听、但不再
  应答"的状态。现在**两条路失败就换、并记住哪条通**；两条都不通时逐条列出原因，
  再给一句人话结论，而不是只丢一个"更新失败"。
- 三条错误码翻译修正（网络类失败不再显示成"未知错误"）。

### 测试

- `cargo test` **272 / 272**（+5：备份的创建 / 列序 / 空文件拦截 / 表数比对 / 幂等）
- 界面烟测 **37 / 37**（+6 备份步骤）
- 联网测试（`--ignored`）：检查更新 + 下载，2 / 2 通过

## [0.3.0-beta.1] - 2026-09-19

**主题：账本进得来，崩了不怕。**

v0.3.0 两大块交付：**Excel / CSV 导入建表**（发起人明确过"不是替代 Excel，
是要能导入 Excel"）与**崩溃恢复**（三条底线里"不丢数据"的最后一块）。
另外把程序内更新的前半段（检查 / 下载 / 校验 / 暂存）打通。

> ⚠️ **这是 beta：安装版、备份、默认模板库还没做。** 完整的 v0.3.0 交付清单
> 见 `local-docs/handoff/VERSION_PLAN.md` 第 3.2 节（本地文档，不随仓库分发）。

### 新增

**Excel / CSV 导入建表**
- 导入向导：铺出前 30 行让用户**点选表头行**（中文台账上面常压着标题行、导出日期行，
  程序猜错了他不一定看得出来）；每列给出**判断依据 + 置信度**（低置信度标红），
  可逐列改名 / 改类型 / 取消勾选
- 导入前先警告有坑的地方：合并单元格、15 位数字被 Excel 截断、1904 日期系统……
- **分批写入由前端驱动**：20 万行也不会让界面连同进度条一起冻住
  （那比没有进度条更糟）；失败或取消**不留半张表**
- 保真：前导零工号保住（001001）、金额按「分」存、11 位电话不变科学计数法
- 实测吞吐：1 万行 **74,627 行/秒** · 10 万行 **125,000 行/秒** ·
  20 万行 **109,230 行/秒**（14.9 MB CSV 写入 1.8 秒，分 40 批）

**崩溃恢复**
- 数据目录里的「未正常退出」标记（启动写、正常退出删）+ 启动完整性自检
  （`quick_check` + WAL checkpoint），自检**在打开数据库之前**跑
- 恢复向导：生成一致性快照（`VACUUM INTO`）/ 打开数据目录 / 知道了。
  **刻意没有「一键修复」** —— 向导的职责是让人看清现场，不是假装能把坏数据变好
- 强杀端到端测试（真实进程 · 三阶段）：写入 → 强杀 → 重启必进向导 + 数据一行不丢 →
  正常退出后**不再误报**。实测已提交 150 行，重启后 150 行

**程序内更新（前半段，ADR-0018）**
- 版本检查：GitHub Releases API，稳定 / 预发布双通道，默认稳定通道
- HTTPS 下载走系统 WinHTTP（不引入 TLS 依赖，体积优先）
- 下载后**五步校验**与暂存；替换动作由新版本 exe 执行（无签名前提下的设计）

**界面自动化测试体系（不替代人，但把能机器化的都机器化了）**
- 界面烟测：真实 WebView 里真实点击 **31 步**（导航 / 更新卡 / 数据库建表录行读回 /
  导入向导 / 进度条三态）
- 端到端验收：**28 步**真实数据走完整导入链路（选文件 → 认表头 → 改列 → 分批落库 →
  左栏出现 → 读回 → 删表）
- 压力测试：**22 项 × 三档**（1 万 / 10 万 / 20 万），含分页不重不漏、排序、筛选、聚合
- 自动点击走查：外部 DevTools 协议驱动 —— **18 张截图** + 逐页文案导出 +
  4 档窄屏（1000/800/640/520）布局体检，产出可审报告

### 变更

- 版本线重排：`v0.3.0` 拆出「Excel 导入建表 / 默认模板库 / 简洁模式 / 教程扩写」
  四项为第一优先级（发起人 2026-09-18 明确要求），表结构编辑与可靠性套件顺延
- **导入的空值语义拍板（Q-047）**：导入时空单元格统一存 `NULL`（**文本列也不例外**）；
  手动编辑保留「文本格清空 = 空串」的既有语义。两条路径的差异是**刻意**的 ——
  源文件的空格子只有一个含义（"没填"），而"空串"是手动编辑时用户能主动表达的意思

### 变更

- **产品定位转向「Office 三件套的扩展与升级 + 办公实用功能集**（ADR-0023）。
  不替代 Office，补它不做与做不好的那块；三件套方向为文档 / 表格 / 演示，底座仍是
  本地优先、隐私优先、单文件、零遥测。
- **教程重写**：从「数据库使用教程」改为「办公套件使用教程」（9 节），
  补上 v0.4.0 的新能力（共通字段 / 同步规则 / 关联字段 / 命名视图）与隐私、备份说明。
- 路线图重排：`ROADMAP.md` 北极星改写，M1 改为「办公台：记与截」，M2 明确为
  「三件套之一：表格」，M2.5 去 SQL 重构标记完成，新增 M2.6（文档与演示方向）。
- 参考文档口径同步：`README.md`、`AGENTS.md`、`docs/03`（一级模块按
  文档 / 表格 / 演示 / 工具 / 设置 组织）。

### 修复

- 「日期时间」字段类型**选了就报错**（serde 序列化成 `date_time`、界面传 `datetime`）
  —— 存在已久，此前没有任何测试用这个类型建过表
- 20 万行导入上限的**边界判错**：闸门按整表行数卡，导致「2 行标题 + 1 行表头 +
  恰好 20 万行数据」被拒 —— 少一行能过、恰好到上限反而不过
- **构建时弹控制台窗口刷乱码**（`spawnSync` 缺 `windowsHide`）—— 用户反馈的现象
- **更新检查在系统代理不可用时整体失败**：系统代理（代理软件）挂着时，
  `AUTOMATIC_PROXY` 把每次请求都交给那个死代理 —— 实测检查更新 5/5 全失败
  （0x80072EFD），而同一时刻直连 api.github.com 完全正常。现在改为**双路兜底**
  （系统代理 → 直连，本次运行内记住哪条通），并且 403/429 也换路重试
  （GitHub 匿名限额按 IP 算，代理出口是共享 IP）。附带修正了**三条错误码翻译**
  —— 12029 被错标成"TLS 握手失败"，把"代理挂了"显示得像"证书出问题"
- 发布工具的「待推送数量」不再依赖 `origin/main` 这个可能不存在的本地引用
- 联网测试不再"随机红"：两条路都是传输层失败时如实打印原因并降级（环境问题），
  HTTP 应答错误与校验失败仍然必须红

### 数字

`cargo test` **267 / 267**（上一版 172）· 编译零警告 · release **6.164 MB** ·
界面烟测 31/31 · 端到端 28/28 · 崩溃恢复 16/16 · 压力 22/22 × 3 档 ·
联网检查（走兜底）实测通过 · 走查 7/7（布局疑似 0 处）· 下载 e2e 在 CDN 不可达时优雅降级 ·
五个门禁全过（接线 / 动效 / 对比度 / 体积 / 字段名）

---

⚠️ **仍然没有代码签名证书** —— 运行时 SmartScreen 会拦（点「更多信息 → 仍要运行」）。
这是已知且不会自动消失的障碍，不假装没有。

⚠️ **界面层的人工走查仍未做**：自动走查把"值得看的东西"整理好了
（截图 / 文案 / 布局疑似清单），但"上手用一遍"替代不了。


## [0.2.1] - 2026-09-19

**主题：修与验。** 把 beta 发出去却没人点过的东西修稳、验实。
`cargo test` **172 全过**（基线 161 → +11）、编译零警告、三个门禁全过、
实机自检 0 资源未命中、release **5.952 MB**。

### 变更

- **产品定位转向「Office 三件套的扩展与升级 + 办公实用功能集**（ADR-0023）。
  不替代 Office，补它不做与做不好的那块；三件套方向为文档 / 表格 / 演示，底座仍是
  本地优先、隐私优先、单文件、零遥测。
- **教程重写**：从「数据库使用教程」改为「办公套件使用教程」（9 节），
  补上 v0.4.0 的新能力（共通字段 / 同步规则 / 关联字段 / 命名视图）与隐私、备份说明。
- 路线图重排：`ROADMAP.md` 北极星改写，M1 改为「办公台：记与截」，M2 明确为
  「三件套之一：表格」，M2.5 去 SQL 重构标记完成，新增 M2.6（文档与演示方向）。
- 参考文档口径同步：`README.md`、`AGENTS.md`、`docs/03`（一级模块按
  文档 / 表格 / 演示 / 工具 / 设置 组织）。

### 修复

- **SQL 耗时完全不可见（P1）**：`app/ui/sql.js` 读的是 `res.elapsedMs`，
  而 Rust 侧 `QueryResult` 序列化出来的是 `elapsed_ms` —— 与 `has_more` / `hasMore`
  是同一类事故：**值恒为 `undefined`，界面毫无异常**。
  后果是每条语句的耗时徽章、结果块的总耗时、以及「超过 1 秒请当心结果不完整」的提醒全部不显示
- **接线门禁的「假绿」**：`check-wiring.cjs` 第 4 项（前端调用的 IPC 命令是否都有分支）
  原先直接扫 `main.rs` 原文，**一句注释里提到 `"schema.runQuery" =>` 就能让它变绿**。
  现在先剥离 Rust 注释（字符串内容保留），并额外认 `cmd == "x.y"` 形式 ——
  门禁该适配代码，而不是逼着代码去迎合门禁

### 新增

- **SQL 执行超时与中断（Q-042）**：`schema.runQuery` 改为**异步**。
  主线程解析参数 → 过危险语句闸门 → 登记 `Connection::interrupt_handle()` → 起工作线程后立刻返回，
  **界面不再被慢查询冻住**。执行中再点一次「执行」= 中断；超过 `timeoutMs`（默认 30 秒）
  由超时哨兵自动按下中断。报错不是「查询失败」四个字，而是把原因（条件列没索引 / 全表扫描）
  和下一步讲清楚。**忙时明确拒绝第二条查询而不是排队** —— 排队会让「哪条结果对应界面上哪个结果块」
  变得不可预测。拿不到事件循环时（如单元测试）明确报错，不静默挂起
- **建表向导的默认值输入框**：`DEFAULT` 白名单早已就绪，这次把界面补上。
  关键词（`CURRENT_TIMESTAMP` / `NULL` / 数字）原样落库，普通文本自动加引号；
  **金额列的默认值按「元」输入、按整数「分」落库**，与金额列本身的存储口径一致
- **数据库页验收自动化（11 个集成测试）**：把 `ENV_SETUP.md` 第 4 节那 16 步验收里
  能用 IPC 表达的部分变成**可重复运行**的测试 —— 建表含金额列 → 金额往返（含三位小数被拒）→
  250 行 keyset 翻页不重不漏 → 三态排序 → 列筛选 → 改单元格与 NULL → 批量删行 →
  危险语句双重闸门（含 `WHERE 1=1` 恒真谓词）→ 忙时拒绝与中断可达 →
  工作区状态「重开」后恢复 → 建表默认值落库。
  走的**是真正的分发入口**（`dispatch`），不是绕过 IPC 直接调内部函数
- **跨语言字段名通用门禁**：原先只盯 `Page` 的 `has_more` 一条，现在对所有 serde `snake_case`
  字段禁止在 `db.js` / `sql.js` 里出现驼峰写法（检查前先剥离注释，免得门禁被自己的说明文字弄红）
- **数据库默认模板**：建表对话框顶部可套用「客户台账 / 进出货 / 费用报销 / 库存盘点」，
  字段与类型已配好（金额按「分」、日期用日期型），表名与字段都能改。不用再从空白表开始发呆
- **`node scripts/check-wiring.cjs` 接线门禁**：检查四项 ——
  ① `index.html` 引用的站内资源是否都在 `assets.rs` 登记
  ② `app/ui/*.js` 是否都挂进了 `index.html`（写了没挂 = 死文件）
  ③ `app/ui/*.css` 是否都进了 `check-motion` 的 TARGETS
  ④ 前端调用的 IPC 命令是否都在 `main.rs` 的 `dispatch()` 里有分支。
  这四项都不是编译错误，只能靠门禁拦（已做负向验证：故意移出一项确实变红）

### 文档

- ADR-0017（AI 协助的隐私边界）：默认本地、不许静默降级到云端、外部调用逐次授权、
  默认只发结构信息不发数据行、AI 生成的内容不自动执行、没做到之前界面不放 AI 入口
- 新增 `local-docs/handoff/ACCEPTANCE-2026-09-19.md`：数据库页 16 步验收记录，
  **逐项标注「已自动化 / 仅 IPC 层 / 未覆盖」**，不把「测过后端」说成「点过界面」

### 已知未完成

- **界面层（真实的鼠标点击路径）仍未经人工走查**：上面的自动化覆盖的是 IPC 与数据库这一层；
  「双击单元格」「勾选两行」「表头点第三次回到无排序」这些**前端状态机**没有被测到
- 16 步验收里的第 16 步（一键创建示例台账）没有测试覆盖
- 14 / 15 步用「换一个 `AppState` 打开同一份库」模拟重启，**真实进程重启未做**
- SQL 执行那条异步链路在单元测试里拿不到事件循环，测到的是闸门与拒绝逻辑，
  **执行与回传需要实机验证**（已实机启动自检通过）
- 其余同 beta.3：无安装版、无签名、无自动更新

---

## [0.2.0-beta.3] - 2026-09-18

围绕「更新与升级时不丢用户数据」的一批，`cargo test` 161 全过、编译零警告、release 5.906 MB。

### 新增

- **数据自动升级**：数据库结构逐级迁移，每级一个事务；**每升一级前用 `VACUUM INTO` 把整份库备份到
  `<数据目录>/backups/before-v<N>-<时间戳>.db`**。备份失败直接中止，迁移失败回滚且 `user_version` 不变，
  报错会指向备份文件位置
- **工作区状态持久化**：当前视图 / 打开的表 / 侧栏状态 / 窗口尺寸存进库，下次启动自动恢复。
  **覆盖 exe 或解压新便携包，这些都不丢**
- **退出前保存**：关闭窗口时先通知前端把没写完的内容落库（400ms 宽限，不会因此关不掉），
  再把工作区状态落盘后退出
- **设置页「数据库使用教程」**：8 节 —— 数据库是什么（白话）/ 三步建一张台账 / 9 种字段类型怎么选 /
  查找与筛选 / 改与删 / 三个能直接抄的 SQL 例子 / 常见错误对照 / 术语表
- **数据库页首次使用引导**：一张表都没有时给出三步引导，并可一键「创建示例台账」立刻看到效果

### 变更

- ADR-0016（程序内更新：默认关闭、显式开启、强制签名校验、更新不碰数据）已提交，**状态：提议，待确认**

### 已知未完成

- **自动更新只有设计没有实现**（ADR-0016 待确认 + 依赖代码签名证书）
- 数据库页的交互验收**仍未人工执行**
- 其余同 beta.2

---

## [0.2.0-beta.2] - 2026-09-18

修掉 beta.1 里两个只有真用起来才会暴露的问题，`cargo test` 153 全过、编译零警告。
**如果已经下载了 beta.1，请换成这一版。**

### 变更

- **产品定位转向「Office 三件套的扩展与升级 + 办公实用功能集**（ADR-0023）。
  不替代 Office，补它不做与做不好的那块；三件套方向为文档 / 表格 / 演示，底座仍是
  本地优先、隐私优先、单文件、零遥测。
- **教程重写**：从「数据库使用教程」改为「办公套件使用教程」（9 节），
  补上 v0.4.0 的新能力（共通字段 / 同步规则 / 关联字段 / 命名视图）与隐私、备份说明。
- 路线图重排：`ROADMAP.md` 北极星改写，M1 改为「办公台：记与截」，M2 明确为
  「三件套之一：表格」，M2.5 去 SQL 重构标记完成，新增 M2.6（文档与演示方向）。
- 参考文档口径同步：`README.md`、`AGENTS.md`、`docs/03`（一级模块按
  文档 / 表格 / 演示 / 工具 / 设置 组织）。

### 修复

- **超过 200 行的表翻不了页**（P1）：`db.js` 把分页应答的 `has_more` 写成了 `hasMore`，
  该值恒为 `undefined`，于是「加载更多」永远不触发。测试、界面自检、日志**全都显示正常** ——
  只有真的打开一张超过一页的表往下滚才会发现
- **危险 SQL 闸门可被 `WHERE 1=1` 绕过**（P2）：`DELETE FROM t WHERE 1=1` 有顶层 `WHERE`，
  此前会被当成"有条件"而不弹确认，直接全表删。`has_where` 现在会评估谓词：
  恒真写法（`1` / `true` / `1=1` / `(1=1)` 及其纯恒真的 AND 组合）一律视为没有过滤条件；
  判断不了时按"需要确认"处理（宁多问一次）
- `refreshTables` 加并发守卫：快速切换视图不再重复拉表列表

### 安全

- 新增回归门禁测试「分页应答的字段名与界面读取的名字一致」：把 Rust 侧序列化出的键
  与 `db.js` 读取的名字两头钉住，**已做负向验证**（改回 `hasMore` 时测试确实变红）。
  跨语言边界传字段这类静默失效，以后不许再靠眼熟

### 已知未完成

同 beta.1；另新增一条：数据库页的交互验收**仍未人工执行**

---

## [0.2.0-beta.1] - 2026-09-18

**数据库模块正式上线 —— 本项目的核心能力（内置 SQL 数据库）从这一版起可用。**
`cargo test` 151 个用例全过、编译零警告、门禁全绿。

### 新增

- **「数据库」页上线**（此前为占位卡片）：
  - 可视化**建表向导**：表名 + 字段列表，9 种字段类型（文本 / 整数 / 小数 / 金额 / 是否 / 日期 / 日期时间 / JSON / 二进制），支持主键与必填
  - **数据网格**（`grid.js`，2,239 行）：虚拟滚动（复用 DOM 行池）、表头排序三态、列筛选（包含匹配，与 keyset 游标分页可叠加）、单元格就地编辑、NULL 与空串分家显示、批量删行、表尾新增行
  - **SQL 编辑器**（`sql.js`，1,947 行）：自实现语法高亮、表名字段名补全侧栏、执行历史、多语句逐条执行
  - **删表**需逐字输入表名确认
- **策略层危险操作闸门**（IPC 边界）：`DROP` / `TRUNCATE` / 无顶层 `WHERE` 的 `DELETE` / `UPDATE` 必须带着显式确认才执行；前端确认框由 `needsConfirm` 应答协议驱动
- `has_where` 升级为**括号深度感知**：`UPDATE t SET a=(SELECT 1 WHERE 1=1)` 这类 WHERE 藏在子查询里的全表改写，现在会被正确拦下确认（第十二轮交接点名的确认漏网）
- 新增 IPC 命令 10 条：`schema.listTables` / `getTable` / `columnMeta` / `createTable` / `dropTable` / `pageRows` / `updateCell` / `deleteRows` / `insertRows` / `runQuery`
- 界面自检覆盖扩展：数据网格 / SQL 编辑器 / 数据库页三个模块纳入 `app.diag`

### 变更

- **产品定位转向「Office 三件套的扩展与升级 + 办公实用功能集**（ADR-0023）。
  不替代 Office，补它不做与做不好的那块；三件套方向为文档 / 表格 / 演示，底座仍是
  本地优先、隐私优先、单文件、零遥测。
- **教程重写**：从「数据库使用教程」改为「办公套件使用教程」（9 节），
  补上 v0.4.0 的新能力（共通字段 / 同步规则 / 关联字段 / 命名视图）与隐私、备份说明。
- 路线图重排：`ROADMAP.md` 北极星改写，M1 改为「办公台：记与截」，M2 明确为
  「三件套之一：表格」，M2.5 去 SQL 重构标记完成，新增 M2.6（文档与演示方向）。
- 参考文档口径同步：`README.md`、`AGENTS.md`、`docs/03`（一级模块按
  文档 / 表格 / 演示 / 工具 / 设置 组织）。

### 修复

- 筛选列不存在时明确报错，不再静默当作"无此条件"

### 性能

- 大表浏览走 **keyset 分页**（上限 10,000 行/页），不用 `OFFSET`；排序键含并列值与 NULL 时不重不漏
- 单次 SQL 结果集上限 5,000 行（可在 1–100,000 之间调），防止 `SELECT *` 卡死界面

### 安全

- 渲染层仍然零数据库直访：所有库表操作经 IPC 白名单命令；表名/字段名过标识符校验后由 Rust 侧转义，值一律参数绑定
- SQL 内容不写日志（隐私承诺不变）；"执行过写操作"这一事实记入审计日志

### 已知未完成

- **表结构编辑**（改表名 / 加列 / 改注释）：后端 API 与测试已就绪并显式标注，界面未做（建表向导二期）
- SQL 执行**没有超时机制**（`get_interrupt_handle` 未接线）—— 慢查询只能等它跑完
- 数据库页的交互（建表 → 翻页 → 编辑 → 筛选 → 删行）**尚未人工过一遍**，自检只证明模块加载成功
- 安装版 / 代码签名 / 自动更新 / 长截图 / 业务表 / AI 功能：见 [0.1.0-alpha.1] 的清单，状态不变

---

## [0.1.0-alpha.1] - 2026-09-17

首个可运行的 alpha。**桌面端垂直切片打通：外壳 / 存储 / 界面 / Excel 双向 /
格式转换 / 截屏 / 命令面板 / 主题与动效 / 导入管线。**

> ⚠️ **这是 alpha，请勿用于唯一的生产数据。** 已知未完成的部分列在本节末尾。

### 新增

**外壳与界面**
- Rust + 系统 WebView2 的桌面外壳（Windows 优先），单文件 exe，无运行时依赖安装
- 界面资源走自定义 `deskbase://` 协议：**渲染层无文件系统直访**，只服务编译期固定的资源表
- **11 个内置主题**，按四个流派家族组织（宣纸 / 青瓷 / 竹青 / 朱墨 / 夜墨 / 松烟 /
  玄铁 / 高对比亮 / 高对比暗 / 等宽绿 / 粗野），主题改的是**结构**而不只是色相
- 四档动效强度（关 / 精简 / 标准 / 丰富），跟随系统的「减少动效」自动封顶
- 标题槽位内嵌**得意黑 Smiley Sans**（SIL OFL 1.1，随程序分发、不联网下载）
- **命令面板**：`Ctrl+K` 唤起，支持**拼音首字母**搜索（`xjbj` → 新建笔记）
- 基础组件库：Toast 栈（可带撤销）、模态框（含焦点陷阱）、骨架屏、开关、工具提示、滚动边缘阴影
- 三条从物理参数采样出的 **CSS `linear()` 真弹簧**曲线（临界阻尼 / ζ=0.85 / ζ=0.70）

**数据**
- SQLite（bundled，WAL + `synchronous=FULL`）单文件存储，软删除
- **Excel 导出**：长编号（订单号 / 身份证 / 银行卡）按文本写入，不会被 Excel 变成科学计数法
- **Excel / CSV 导入前的静默毁数据检测**，逐个点名这七类坑：
  Excel 的 15 位有效数字截断、1904 日期系统（差 1462 天）、1900 假闰日、
  `<dimension>` 炸弹、合并单元格、公式缓存值过期、全角数字与 GBK 编码
- **CSV 编码探测**：BOM → 严格 UTF-8 校验 → GB18030 回退。
  专门为「中文 Excel 的另存为 CSV 输出的是 GBK 不是 UTF-8」这一条设计
- 导入落库管线：分批提交、进度与数据同事务、**可撤销**、跨进程强杀后可报告进度

**办公工具**
- **格式转换**（纯离线）：xlsx ↔ csv/tsv、png ↔ jpg/bmp/webp、GBK ↔ UTF-8 ↔ UTF-16。
  **两步式**：先给出「会丢掉什么」的清单（具体到数量与名字），确认后再执行
- **截屏**：按物理像素抓取，DPI 感知（125% / 150% 下不模糊）
- **批量导入**：从表格批量建笔记

### 安全

- **零出网、零遥测**。唯一的联网能力是「用系统浏览器打开项目仓库 / 发布页」，
  地址由 Rust 侧**硬编码白名单**限定，越界的请求会被拒绝并写进审计日志
- 文件路径只在 Rust 侧流转：原生对话框在 Rust 里打开，渲染层既给不了路径也拿不到路径，
  因此**渲染层没有「读任意文件」的能力**
- 所有转换与导出**只写新文件**，已有文件一律不覆盖，绝不原地修改用户的原件
- 截图内容不上传、不记录，日志里只有尺寸

### 已知未完成（这一版明确没有的东西）

- **没有安装包**。首发只出便携版。自建安装器涉及注册表写入、卸载残留、
  WebView2 bootstrapper 探测，都有明确验收标准，没达标就不发
- **没有代码签名证书**，所以 Windows SmartScreen 会拦安装包
  （便携版受影响较小）。这也是将来更新器会被 Defender 误报的唯一真正解法
- **没有自动更新**。更新是「打开系统浏览器 → 到发布页手动下载」
- **长截图的滚动拼接还没接线**。抓屏与拼接算法（含相位相关的位移估计）
  已经写好并有测试，缺的是「界面滚一屏 → Rust 抓一帧」的往返协议
- **数据库模块只落地了笔记**。物流 / 财务 / 工厂收发那些场景的表
  （单据明细、库存流水、月结快照）设计已完成但尚未建表
- **AI 功能一个都没接**。本地语义搜索、OCR、分类打标签的选型与隐私
  方案已经定稿，实现排在后续版本
- 字体自定义导入（.ttf / .otf / .woff2）界面还没做，目前只有内置预设

### 工程

- `cargo test` **103 个用例**，覆盖 Excel 往返、CSV 编码、格式转换、
  截屏拼接、崩溃恢复
- 四道 CI 门禁：测试、动效合规（只动 transform/opacity）、
  主题对比度（11 主题 × 15 组 WCAG）、界面脚本语法
- 编译零警告，且 CI 用 `-D warnings` 把它变成硬失败
- 发布脚本产出便携 zip + SHA-256 + **CycloneDX SBOM**（149 个组件，
  按 Windows 目标平台过滤，不是全平台并集）

## 版本发布规则说明

- 项目已发布首个 alpha（`0.1.0-alpha.1`，2026-09-17）。**alpha 不算「正式版本」**：
  `0.x` 期间不承诺向后兼容，数据结构与界面都可能变。
- 正式版本发布后，本文件将按下述结构记录：

  ```markdown
  ## [1.2.0] - 2026-06-01

  ### 新增
  - ...

  ### 变更

- **产品定位转向「Office 三件套的扩展与升级 + 办公实用功能集**（ADR-0023）。
  不替代 Office，补它不做与做不好的那块；三件套方向为文档 / 表格 / 演示，底座仍是
  本地优先、隐私优先、单文件、零遥测。
- **教程重写**：从「数据库使用教程」改为「办公套件使用教程」（9 节），
  补上 v0.4.0 的新能力（共通字段 / 同步规则 / 关联字段 / 命名视图）与隐私、备份说明。
- 路线图重排：`ROADMAP.md` 北极星改写，M1 改为「办公台：记与截」，M2 明确为
  「三件套之一：表格」，M2.5 去 SQL 重构标记完成，新增 M2.6（文档与演示方向）。
- 参考文档口径同步：`README.md`、`AGENTS.md`、`docs/03`（一级模块按
  文档 / 表格 / 演示 / 工具 / 设置 组织）。

### 修复
  - ...

  ### 安全
  - ...

  ### 性能
  - 万行表格滚动帧率由 42 FPS 提升至 58 FPS（#789）

  ### 破坏性变更
  - 插件 API `host.db.exec` 已重命名为 `host.db.query`，旧名将在 2.0 移除（#790）
  ```

### 破坏性变更的声明要求

任何 `MAJOR` 版本或含破坏性变更的发布，必须：

1. 在 `CHANGELOG.md` 中以 **破坏性变更** 小节明确列出；
2. 在对应文档中给出**迁移指南**（旧格式 → 新格式、旧 API → 新 API）；
3. 至少提前 **一个次版本** 公告，并在运行时给出可操作的迁移提示；
4. 数据格式变更必须提供**自动迁移 + 迁移前自动备份 + 迁移失败回滚**。

---

## 链接

- [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)
- [语义化版本](https://semver.org/lang/zh-CN/)
- [项目路线图](ROADMAP.md)
- [贡献指南](CONTRIBUTING.md)
